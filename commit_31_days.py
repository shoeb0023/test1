#!/usr/bin/env python3
"""
31-Day JavaScript Mastery Series - Automated Git Committer & GitHub Publisher
Author: shoeb0023 <sshoeb0023@gmail.com>
Target Range: January 1, 2026 - January 31, 2026

Usage:
    python commit_31_days.py                  # Commit all 31 days backdated
    python commit_31_days.py --push --force   # Commit and force push to GitHub origin/main
    python commit_31_days.py --push-only --force  # Force push existing commits to origin/main
    python commit_31_days.py --dry-run        # Preview commit sequence without touching git
"""

import argparse
import json
import os
import shutil
import subprocess
import sys
from pathlib import Path

# Payload containing all 31 days code, tests, and documentation
PAYLOAD = json.loads('{\n  "days_1_to_29": [\n    [\n      1,\n      "feat: implement custom polyfill for Array.prototype.map",\n      {\n        "days/day01-map-polyfill/map.js": "/**\\n * Custom Polyfill for Array.prototype.map\\n * Specification: ECMA-262 22.1.3.16 Array.prototype.map(callbackfn [ , thisArg ])\\n */\\n\\nfunction myMap(callback, thisArg) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myMap called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0; // Unsigned 32-bit integer conversion\\n  const A = new Array(len);\\n\\n  for (let k = 0; k < len; k++) {\\n    // Only invoke callback if index k exists in O (sparse array handling)\\n    if (k in O) {\\n      A[k] = callback.call(thisArg, O[k], k, O);\\n    }\\n  }\\n\\n  return A;\\n}\\n\\nArray.prototype.myMap = myMap;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myMap ---\');\\n  \\n  const numbers = [1, 2, 3, 4];\\n  const doubled = numbers.myMap((num, idx) => num * 2 + idx);\\n  console.assert(JSON.stringify(doubled) === JSON.stringify([2, 5, 8, 11]), \'Basic mapping failed\');\\n\\n  // Sparse array handling\\n  const sparse = [1, , 3]; // eslint-disable-line no-sparse-arrays\\n  const sparseMapped = sparse.myMap(x => x * 2);\\n  console.assert(0 in sparseMapped && !(1 in sparseMapped) && 2 in sparseMapped, \'Sparse mapping failed\');\\n  console.assert(sparseMapped[0] === 2 && sparseMapped[2] === 6, \'Sparse values mismatch\');\\n\\n  // thisArg binding\\n  const multiplier = {\\n    factor: 10,\\n    multiply(n) { return n * this.factor; }\\n  };\\n  const scaled = [2, 3].myMap(function(n) { return this.multiply(n); }, multiplier);\\n  console.assert(JSON.stringify(scaled) === JSON.stringify([20, 30]), \'thisArg binding failed\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myMap tests passed!\');\\n}\\n\\nmodule.exports = { myMap };\\n"\n      }\n    ],\n    [\n      2,\n      "feat: implement custom polyfill for Array.prototype.filter",\n      {\n        "days/day02-filter-polyfill/filter.js": "/**\\n * Custom Polyfill for Array.prototype.filter\\n * Specification: ECMA-262 22.1.3.7 Array.prototype.filter(callbackfn [ , thisArg ])\\n */\\n\\nfunction myFilter(callback, thisArg) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myFilter called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0;\\n  const result = [];\\n\\n  for (let k = 0; k < len; k++) {\\n    if (k in O) {\\n      const val = O[k];\\n      if (callback.call(thisArg, val, k, O)) {\\n        result.push(val);\\n      }\\n    }\\n  }\\n\\n  return result;\\n}\\n\\nArray.prototype.myFilter = myFilter;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myFilter ---\');\\n\\n  const numbers = [1, 2, 3, 4, 5, 6];\\n  const evens = numbers.myFilter(n => n % 2 === 0);\\n  console.assert(JSON.stringify(evens) === JSON.stringify([2, 4, 6]), \'Filter evens failed\');\\n\\n  // Sparse array handling (skipped empty slots)\\n  const sparse = [10, , 25, , 30]; // eslint-disable-line no-sparse-arrays\\n  const filteredSparse = sparse.myFilter(n => n > 15);\\n  console.assert(JSON.stringify(filteredSparse) === JSON.stringify([25, 30]), \'Sparse filtering failed\');\\n\\n  // Context thisArg validation\\n  const validator = {\\n    threshold: 3,\\n    isAbove(val) { return val > this.threshold; }\\n  };\\n  const filteredWithThis = [1, 2, 4, 5].myFilter(function(n) { return this.isAbove(n); }, validator);\\n  console.assert(JSON.stringify(filteredWithThis) === JSON.stringify([4, 5]), \'thisArg filter failed\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myFilter tests passed!\');\\n}\\n\\nmodule.exports = { myFilter };\\n"\n      }\n    ],\n    [\n      3,\n      "feat: implement custom polyfill for Array.prototype.reduce",\n      {\n        "days/day03-reduce-polyfill/reduce.js": "/**\\n * Custom Polyfill for Array.prototype.reduce\\n * Specification: ECMA-262 22.1.3.18 Array.prototype.reduce(callbackfn [ , initialValue ])\\n */\\n\\nfunction myReduce(callback, initialValue) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myReduce called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0;\\n  let k = 0;\\n  let accumulator;\\n\\n  // Determine accumulator and start index\\n  if (arguments.length >= 2) {\\n    accumulator = initialValue;\\n  } else {\\n    let kPresent = false;\\n    while (k < len && !kPresent) {\\n      if (k in O) {\\n        accumulator = O[k];\\n        kPresent = true;\\n      }\\n      k++;\\n    }\\n    if (!kPresent) {\\n      throw new TypeError(\'Reduce of empty array with no initial value\');\\n    }\\n  }\\n\\n  while (k < len) {\\n    if (k in O) {\\n      accumulator = callback(accumulator, O[k], k, O);\\n    }\\n    k++;\\n  }\\n\\n  return accumulator;\\n}\\n\\nArray.prototype.myReduce = myReduce;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myReduce ---\');\\n\\n  const sum = [1, 2, 3, 4].myReduce((acc, curr) => acc + curr, 0);\\n  console.assert(sum === 10, \'Basic sum reduce failed\');\\n\\n  const product = [2, 3, 4].myReduce((acc, curr) => acc * curr);\\n  console.assert(product === 24, \'Reduce without initialValue failed\');\\n\\n  // Object tally accumulation\\n  const fruits = [\'apple\', \'banana\', \'apple\', \'orange\', \'banana\', \'apple\'];\\n  const tally = fruits.myReduce((acc, fruit) => {\\n    acc[fruit] = (acc[fruit] || 0) + 1;\\n    return acc;\\n  }, {});\\n  console.assert(tally.apple === 3 && tally.banana === 2 && tally.orange === 1, \'Tally reduce failed\');\\n\\n  // Empty array without initial value check\\n  let threw = false;\\n  try {\\n    [].myReduce(x => x);\\n  } catch (e) {\\n    threw = e instanceof TypeError;\\n  }\\n  console.assert(threw, \'Expected TypeError on empty array with no initial value\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myReduce tests passed!\');\\n}\\n\\nmodule.exports = { myReduce };\\n"\n      }\n    ],\n    [\n      4,\n      "docs: add explanations and visual flow for JavaScript Execution Context and Call Stack",\n      {\n        "docs/day04-execution-context.md": "# JavaScript Execution Context and Call Stack\\n\\n## 1. What is an Execution Context (EC)?\\nEvery time JavaScript code runs, it does so within an **Execution Context**. An Execution Context is the conceptual environment in which JavaScript evaluates and executes code.\\n\\nThere are three primary types of Execution Contexts:\\n1. **Global Execution Context (GEC)**: Created by default upon script initialization. In browsers, it attaches to `window`; in Node.js, to `global`. Only one GEC exists.\\n2. **Function Execution Context (FEC)**: Created whenever a function is *invoked* (not when declared).\\n3. **Eval Execution Context**: Created when code is executed inside `eval()`.\\n\\n---\\n\\n## 2. The Two Phases of Execution Context Creation\\n\\n```\\n\\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510\\n\\u2502                   EXECUTION CONTEXT CREATION                \\u2502\\n\\u251c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u252c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2524\\n\\u2502 1. MEMORY CREATION PHASE     \\u2502 2. CODE EXECUTION PHASE      \\u2502\\n\\u2502  - Hoists variable declarations\\u2502  - Executes code line by line\\u2502\\n\\u2502    (var initialized as undef) \\u2502  - Evaluates values & assigns\\u2502\\n\\u2502  - Stores function bodies in \\u2502  - Invokes functions         \\u2502\\n\\u2502    full                      \\u2502  - Handles context switches  \\u2502\\n\\u2502  - Sets up scope chain       \\u2502                              \\u2502\\n\\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2534\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518\\n```\\n\\n### Phase 1: Memory Creation Phase (Creation Phase)\\n- The JS engine scans the code before executing a single line.\\n- Allocates memory for variables and functions.\\n- `var` variables are allocated and initialized to `undefined`.\\n- `let` and `const` variables are allocated in uninitialized state (Temporal Dead Zone - TDZ).\\n- Function declarations are placed in memory with their complete definition.\\n\\n### Phase 2: Code Execution Phase\\n- Code is executed synchronously line-by-line.\\n- Variable assignments are evaluated.\\n- Function calls create new Function Execution Contexts pushed onto the Call Stack.\\n\\n---\\n\\n## 3. Call Stack Flow (LIFO - Last In, First Out)\\n\\n```\\n  Function calls:\\n  a() -> calls b() -> calls c()\\n\\n  Call Stack:\\n  \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510\\n  \\u2502   c() FEC    \\u2502  <-- Top of stack (executing)\\n  \\u251c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2524\\n  \\u2502   b() FEC    \\u2502\\n  \\u251c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2524\\n  \\u2502   a() FEC    \\u2502\\n  \\u251c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2524\\n  \\u2502     GEC      \\u2502  <-- Base of stack (Global)\\n  \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518\\n```\\n\\nWhen `c()` finishes, its context is popped off the stack, control returns to `b()`, then `a()`, and finally back to `GEC`.\\n\\n---\\n\\n## 4. Stack Overflow\\nWhen recursion lacks an exit condition, contexts are pushed onto the Call Stack indefinitely until memory is exhausted:\\n```javascript\\nfunction infinite() {\\n  infinite(); // RangeError: Maximum call stack size exceeded\\n}\\n```\\n"\n      }\n    ],\n    [\n      5,\n      "feat: write implementation examples demonstrating Hoisting (var vs let/const)",\n      {\n        "days/day05-hoisting/hoisting.js": "/**\\n * Hoisting Demonstration: var vs let/const & Functions\\n */\\n\\nfunction demonstrateVarHoisting() {\\n  // var is hoisted and initialized to undefined\\n  console.log(\'var before declaration:\', hoistedVar); // undefined\\n  var hoistedVar = \'I am var\';\\n  console.log(\'var after declaration:\', hoistedVar);   // \'I am var\'\\n  return hoistedVar;\\n}\\n\\nfunction demonstrateTDZ() {\\n  // let and const are hoisted but uninitialized (in TDZ)\\n  let result = \'TDZ Active\';\\n  try {\\n    // Attempting to access tdzLet here throws ReferenceError\\n    // console.log(tdzLet);\\n    eval(\'console.log(tdzLet)\');\\n  } catch (err) {\\n    result = err.name; // ReferenceError\\n  }\\n  let tdzLet = \'Safe now\';\\n  return { result, tdzLet };\\n}\\n\\nfunction demonstrateFunctionHoisting() {\\n  // Function declarations are hoisted completely with body\\n  const fnResult = declaredFunction(); // works!\\n\\n  function declaredFunction() {\\n    return \'Function declaration hoisted!\';\\n  }\\n\\n  // Function expressions with var are hoisted as undefined\\n  let expressionError = null;\\n  try {\\n    expressionFunction(); // TypeError: expressionFunction is not a function\\n  } catch (err) {\\n    expressionError = err.name;\\n  }\\n\\n  var expressionFunction = function() {\\n    return \'Function expression\';\\n  };\\n\\n  return { fnResult, expressionError, expressionFunction: expressionFunction() };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Hoisting Scenarios ---\');\\n\\n  const varRes = demonstrateVarHoisting();\\n  console.assert(varRes === \'I am var\', \'Var hoisting test failed\');\\n\\n  const tdzRes = demonstrateTDZ();\\n  console.assert(tdzRes.result === \'ReferenceError\', \'TDZ ReferenceError test failed\');\\n  console.assert(tdzRes.tdzLet === \'Safe now\', \'TDZ post declaration failed\');\\n\\n  const fnRes = demonstrateFunctionHoisting();\\n  console.assert(fnRes.fnResult === \'Function declaration hoisted!\', \'Function declaration hoisting failed\');\\n  console.assert(fnRes.expressionError === \'TypeError\', \'Function expression error failed\');\\n\\n  console.log(\'\\u2713 All Hoisting demonstrations passed!\');\\n}\\n\\nmodule.exports = {\\n  demonstrateVarHoisting,\\n  demonstrateTDZ,\\n  demonstrateFunctionHoisting,\\n};\\n"\n      }\n    ],\n    [\n      6,\n      "feat: implement Closures with practical data privacy use cases (module pattern)",\n      {\n        "days/day06-closures/closure-privacy.js": "/**\\n * Closures with Practical Data Privacy: Bank Account Module Pattern\\n */\\n\\nfunction createBankAccount(initialBalance, accountHolder) {\\n  // Private variables encapsulated within the outer lexical scope\\n  let balance = Math.max(0, initialBalance || 0);\\n  const owner = accountHolder;\\n  const transactionHistory = [];\\n\\n  function recordTransaction(type, amount) {\\n    transactionHistory.push({\\n      timestamp: new Date().toISOString(),\\n      type,\\n      amount,\\n      balanceAfter: balance,\\n    });\\n  }\\n\\n  recordTransaction(\'OPEN_ACCOUNT\', balance);\\n\\n  // Return public interface (revealing module pattern)\\n  return {\\n    getAccountHolder() {\\n      return owner;\\n    },\\n    getBalance() {\\n      return balance;\\n    },\\n    deposit(amount) {\\n      if (typeof amount !== \'number\' || amount <= 0) {\\n        throw new Error(\'Deposit amount must be a positive number\');\\n      }\\n      balance += amount;\\n      recordTransaction(\'DEPOSIT\', amount);\\n      return balance;\\n    },\\n    withdraw(amount) {\\n      if (typeof amount !== \'number\' || amount <= 0) {\\n        throw new Error(\'Withdrawal amount must be a positive number\');\\n      }\\n      if (amount > balance) {\\n        throw new Error(\'Insufficient funds\');\\n      }\\n      balance -= amount;\\n      recordTransaction(\'WITHDRAW\', amount);\\n      return balance;\\n    },\\n    getTransactions() {\\n      // Return a defensive copy to prevent external mutation\\n      return [...transactionHistory];\\n    }\\n  };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Closures & Data Privacy ---\');\\n\\n  const account = createBankAccount(100, \'Alice\');\\n\\n  // Verify internal variables are completely inaccessible\\n  console.assert(account.balance === undefined, \'balance should be private\');\\n  console.assert(account.owner === undefined, \'owner should be private\');\\n  console.assert(account.transactionHistory === undefined, \'transactionHistory should be private\');\\n\\n  // Test transactions\\n  account.deposit(50);\\n  console.assert(account.getBalance() === 150, \'Deposit failed\');\\n\\n  account.withdraw(30);\\n  console.assert(account.getBalance() === 120, \'Withdraw failed\');\\n\\n  let failedWithdraw = false;\\n  try {\\n    account.withdraw(500);\\n  } catch (err) {\\n    failedWithdraw = true;\\n  }\\n  console.assert(failedWithdraw, \'Overdraft should fail\');\\n\\n  const txs = account.getTransactions();\\n  console.assert(txs.length === 3, \'Transaction count mismatch\');\\n\\n  console.log(\'\\u2713 All Closure data privacy tests passed!\');\\n}\\n\\nmodule.exports = { createBankAccount };\\n"\n      }\n    ],\n    [\n      7,\n      "refactor: optimize closure-based memoization utility function",\n      {\n        "days/day07-memoize/memoize.js": "/**\\n * Optimized Closure-based Memoization Utility\\n * Supports custom key serialization, cache size limit (LRU eviction), and hit statistics.\\n */\\n\\nfunction memoize(fn, options = {}) {\\n  const {\\n    maxSize = 100,\\n    resolver = (...args) => {\\n      if (args.length === 0) return \'__empty__\';\\n      if (args.length === 1 && (typeof args[0] === \'number\' || typeof args[0] === \'string\')) {\\n        return args[0];\\n      }\\n      return JSON.stringify(args);\\n    }\\n  } = options;\\n\\n  // Map maintains insertion order for LRU cache eviction\\n  const cache = new Map();\\n  let hits = 0;\\n  let misses = 0;\\n\\n  const memoized = function(...args) {\\n    const key = resolver(...args);\\n\\n    if (cache.has(key)) {\\n      hits++;\\n      const val = cache.get(key);\\n      // Refresh order for LRU\\n      cache.delete(key);\\n      cache.set(key, val);\\n      return val;\\n    }\\n\\n    misses++;\\n    const result = fn.apply(this, args);\\n\\n    if (cache.size >= maxSize) {\\n      // Evict oldest item (first entry in Map)\\n      const oldestKey = cache.keys().next().value;\\n      cache.delete(oldestKey);\\n    }\\n\\n    cache.set(key, result);\\n    return result;\\n  };\\n\\n  memoized.getStats = () => ({ hits, misses, size: cache.size });\\n  memoized.clear = () => {\\n    cache.clear();\\n    hits = 0;\\n    misses = 0;\\n  };\\n\\n  return memoized;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Optimized Memoize ---\');\\n\\n  let callCount = 0;\\n  const fib = memoize((n) => {\\n    callCount++;\\n    if (n <= 1) return n;\\n    return fib(n - 1) + fib(n - 2);\\n  });\\n\\n  const res1 = fib(10);\\n  console.assert(res1 === 55, \'Fib calculation failed\');\\n  const stats1 = fib.getStats();\\n  console.assert(stats1.hits > 0, \'Cache hits expected\');\\n\\n  // Test custom serializer and LRU eviction\\n  let squareCalls = 0;\\n  const square = memoize((x) => {\\n    squareCalls++;\\n    return x * x;\\n  }, { maxSize: 2 });\\n\\n  square(2); // miss, size 1 [2]\\n  square(3); // miss, size 2 [2, 3]\\n  square(2); // hit, size 2  [3, 2]\\n  square(4); // miss, size 2 (evicts 3) [2, 4]\\n\\n  console.assert(squareCalls === 3, \'LRU square calls mismatch\');\\n  console.assert(square.getStats().hits === 1, \'LRU hits mismatch\');\\n\\n  console.log(\'\\u2713 All Memoize tests passed!\');\\n}\\n\\nmodule.exports = { memoize };\\n"\n      }\n    ],\n    [\n      8,\n      "docs: contrast Scope Chain and Lexical Scoping with code snippets",\n      {\n        "docs/day08-scope-chain.md": "# Scope Chain vs Lexical Scoping in JavaScript\\n\\n## 1. Lexical Scoping (Static Scoping)\\n**Lexical scope** means that the accessibility of variables is determined strictly by the physical location of variables and blocks of code within the source code during write-time (compilation), **not** by where or how functions are called at runtime.\\n\\n```javascript\\nconst globalVar = \'Global\';\\n\\nfunction outer() {\\n  const outerVar = \'Outer\';\\n\\n  function inner() {\\n    // Lexically enclosed inside outer and global\\n    console.log(outerVar);  // Resolves to \'Outer\'\\n    console.log(globalVar); // Resolves to \'Global\'\\n  }\\n\\n  return inner;\\n}\\n\\nconst myFn = outer();\\nmyFn(); // Still resolves outerVar even though executed in global scope!\\n```\\n\\n---\\n\\n## 2. The Scope Chain\\nWhen JavaScript needs to resolve an identifier (variable lookup), it searches the current Lexical Environment Record. If not found, it traverses upward along the **outer reference** link to parent lexical environments until it reaches the Global Environment. If not found in the Global scope, it throws a `ReferenceError`.\\n\\n```\\n  [ inner() Lexical Environment ]\\n       \\u2502  outer reference\\n       \\u25bc\\n  [ outer() Lexical Environment ]\\n       \\u2502  outer reference\\n       \\u25bc\\n  [ Global Lexical Environment ]\\n       \\u2502\\n       \\u25bc\\n  (null / End of Chain -> ReferenceError)\\n```\\n\\n---\\n\\n## 3. Variable Shadowing\\nWhen an inner scope defines a variable with the same identifier as an outer scope, the inner variable **shadows** the outer one:\\n\\n```javascript\\nlet x = 10;\\n\\nfunction demo() {\\n  let x = 20; // Shadows global x\\n  if (true) {\\n    let x = 30; // Shadows function-scoped x\\n    console.log(x); // 30\\n  }\\n  console.log(x); // 20\\n}\\n\\ndemo();\\nconsole.log(x); // 10\\n```\\n\\n---\\n\\n## 4. Key Takeaways\\n- **Lexical Scope**: WHERE function is written.\\n- **Scope Chain**: The PATH taken to look up variables from inner to outer.\\n- Functions capture their surrounding lexical environment at declaration time.\\n"\n      }\n    ],\n    [\n      9,\n      "feat: implement custom debounce function with leading and trailing options",\n      {\n        "days/day09-debounce/debounce.js": "/**\\n * Custom Debounce Implementation with Leading and Trailing Options\\n */\\n\\nfunction debounce(fn, wait = 0, options = {}) {\\n  let timerId = null;\\n  let lastArgs = null;\\n  let lastThis = null;\\n  let result;\\n\\n  const leading = Boolean(options.leading);\\n  const trailing = \'trailing\' in options ? Boolean(options.trailing) : true;\\n\\n  function invoke(time) {\\n    const args = lastArgs;\\n    const thisArg = lastThis;\\n    lastArgs = null;\\n    lastThis = null;\\n    result = fn.apply(thisArg, args);\\n    return result;\\n  }\\n\\n  function debounced(...args) {\\n    lastArgs = args;\\n    lastThis = this;\\n\\n    const isInvokingLeading = leading && !timerId;\\n\\n    if (timerId) {\\n      clearTimeout(timerId);\\n    }\\n\\n    timerId = setTimeout(() => {\\n      timerId = null;\\n      if (trailing && !isInvokingLeading) {\\n        invoke();\\n      }\\n    }, wait);\\n\\n    if (isInvokingLeading) {\\n      return invoke();\\n    }\\n\\n    return result;\\n  }\\n\\n  debounced.cancel = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n    }\\n    lastArgs = null;\\n    lastThis = null;\\n  };\\n\\n  debounced.flush = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n      return invoke();\\n    }\\n    return result;\\n  };\\n\\n  debounced.pending = () => Boolean(timerId);\\n\\n  return debounced;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Debounce Function ---\');\\n\\n  let callCount = 0;\\n  const increment = debounce(() => {\\n    callCount++;\\n  }, 50);\\n\\n  increment();\\n  increment();\\n  increment();\\n  console.assert(callCount === 0, \'Should not have fired synchronously\');\\n\\n  setTimeout(() => {\\n    console.assert(callCount === 1, \'Should fire exactly once after wait\');\\n\\n    // Test leading option\\n    let leadingCalls = 0;\\n    const leadingFn = debounce(() => leadingCalls++, 50, { leading: true, trailing: false });\\n    leadingFn();\\n    leadingFn();\\n    console.assert(leadingCalls === 1, \'Leading call must fire immediately\');\\n\\n    console.log(\'\\u2713 All Debounce tests passed!\');\\n  }, 100);\\n}\\n\\nmodule.exports = { debounce };\\n"\n      }\n    ],\n    [\n      10,\n      "feat: implement custom throttle function using timestamp and setTimeout approaches",\n      {\n        "days/day10-throttle/throttle.js": "/**\\n * Custom Throttle Function\\n * Combines timestamp and setTimeout approaches to guarantee leading and trailing execution control.\\n */\\n\\nfunction throttle(fn, wait, options = {}) {\\n  let timerId = null;\\n  let lastExecTime = 0;\\n  let lastArgs = null;\\n  let lastThis = null;\\n\\n  const leading = \'leading\' in options ? Boolean(options.leading) : true;\\n  const trailing = \'trailing\' in options ? Boolean(options.trailing) : true;\\n\\n  function executeTrailing() {\\n    lastExecTime = leading ? Date.now() : 0;\\n    timerId = null;\\n    fn.apply(lastThis, lastArgs);\\n    lastArgs = null;\\n    lastThis = null;\\n  }\\n\\n  function throttled(...args) {\\n    const now = Date.now();\\n\\n    if (!lastExecTime && !leading) {\\n      lastExecTime = now;\\n    }\\n\\n    const remaining = wait - (now - lastExecTime);\\n\\n    lastArgs = args;\\n    lastThis = this;\\n\\n    if (remaining <= 0 || remaining > wait) {\\n      if (timerId) {\\n        clearTimeout(timerId);\\n        timerId = null;\\n      }\\n      lastExecTime = now;\\n      fn.apply(lastThis, lastArgs);\\n      lastArgs = null;\\n      lastThis = null;\\n    } else if (!timerId && trailing) {\\n      timerId = setTimeout(executeTrailing, remaining);\\n    }\\n  }\\n\\n  throttled.cancel = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n    }\\n    lastExecTime = 0;\\n    lastArgs = null;\\n    lastThis = null;\\n  };\\n\\n  return throttled;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Throttle Function ---\');\\n\\n  let executionCounter = 0;\\n  const throttledFn = throttle(() => {\\n    executionCounter++;\\n  }, 50);\\n\\n  throttledFn(); // immediate execution (leading)\\n  throttledFn(); // ignored\\n  throttledFn(); // ignored\\n  console.assert(executionCounter === 1, \'Leading execution must occur immediately\');\\n\\n  setTimeout(() => {\\n    // After 70ms, trailing call should have fired\\n    console.assert(executionCounter === 2, \'Trailing execution should have fired\');\\n    console.log(\'\\u2713 All Throttle tests passed!\');\\n  }, 100);\\n}\\n\\nmodule.exports = { throttle };\\n"\n      }\n    ],\n    [\n      11,\n      "docs: document how the Event Loop, Microtask Queue, and Callback Queue work",\n      {\n        "docs/day11-event-loop.md": "# JavaScript Event Loop, Microtask Queue & Callback Queue\\n\\n## 1. Concurrency Model & Architecture\\nJavaScript is single-threaded and has a single Call Stack. To handle non-blocking asynchronous operations (network requests, timers, user interactions), it offloads operations to the runtime host environment (browser Web APIs or Node.js libuv).\\n\\n```\\n\\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510\\n\\u2502                         CALL STACK                          \\u2502\\n\\u2502                      (executing code)                       \\u2502\\n\\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u252c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518\\n                               \\u2502 (Call Stack empties)\\n                               \\u25bc\\n\\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510\\n\\u2502                      MICROTASK QUEUE                        \\u2502\\n\\u2502   (Promise.then, queueMicrotask, process.nextTick)          \\u2502\\n\\u2502   * EMPTIED COMPLETELY BEFORE NEXT MACROTASK RUNS *         \\u2502\\n\\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u252c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518\\n                               \\u2502 (When microtasks empty)\\n                               \\u25bc\\n\\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510\\n\\u2502                MACROTASK / CALLBACK QUEUE                   \\u2502\\n\\u2502           (setTimeout, setInterval, setImmediate, I/O)      \\u2502\\n\\u2502   * ONE TASK PROCESSED PER EVENT LOOP TICK *                 \\u2502\\n\\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518\\n```\\n\\n---\\n\\n## 2. Microtasks vs Macrotasks\\n\\n| Feature | Microtask | Macrotask (Callback) |\\n|:---|:---|:---|\\n| **Sources** | `Promise.then/catch/finally`, `queueMicrotask()`, `MutationObserver` | `setTimeout`, `setInterval`, `setImmediate` (Node), I/O events |\\n| **Priority** | High (Priority drain) | Standard |\\n| **Execution Policy** | Runs until empty, including nested microtasks queued during execution | Exactly one macrotask per tick before checking microtasks again |\\n\\n---\\n\\n## 3. Microtask Starvation\\nBecause the runtime completely flushes the microtask queue before rendering or picking the next macrotask, an infinite recursive microtask loop will freeze the entire UI/event loop:\\n\\n```javascript\\n// DANGER: Will starve the event loop!\\nfunction starve() {\\n  Promise.resolve().then(starve);\\n}\\n```\\n"\n      }\n    ],\n    [\n      12,\n      "feat: code examples tracing asynchronous execution order (Promise vs setTimeout)",\n      {\n        "days/day12-async-order/execution-order.js": "/**\\n * Tracing Asynchronous Execution Order: Promises, Microtasks, and Macrotasks\\n */\\n\\nfunction runExecutionOrderQuiz() {\\n  const output = [];\\n\\n  output.push(\'1: Synchronous Start\');\\n\\n  setTimeout(() => {\\n    output.push(\'2: setTimeout 0ms\');\\n  }, 0);\\n\\n  Promise.resolve()\\n    .then(() => {\\n      output.push(\'3: Microtask 1 (Promise)\');\\n      return \'Microtask 2 Chained\';\\n    })\\n    .then((val) => {\\n      output.push(\'4: \' + val);\\n    });\\n\\n  queueMicrotask(() => {\\n    output.push(\'5: queueMicrotask\');\\n  });\\n\\n  output.push(\'6: Synchronous End\');\\n\\n  return output;\\n}\\n\\n// Trace simulation helper for verification\\nfunction simulateOrder() {\\n  return new Promise((resolve) => {\\n    const trace = [];\\n\\n    trace.push(\'sync 1\');\\n\\n    setTimeout(() => {\\n      trace.push(\'macro timeout\');\\n      resolve(trace);\\n    }, 0);\\n\\n    Promise.resolve().then(() => {\\n      trace.push(\'micro promise 1\');\\n    }).then(() => {\\n      trace.push(\'micro promise 2\');\\n    });\\n\\n    trace.push(\'sync 2\');\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Asynchronous Execution Order ---\');\\n\\n  const syncLog = runExecutionOrderQuiz();\\n  console.assert(syncLog[0] === \'1: Synchronous Start\', \'Start order failed\');\\n  console.assert(syncLog[1] === \'6: Synchronous End\', \'Sync end order failed\');\\n\\n  simulateOrder().then((trace) => {\\n    console.assert(\\n      JSON.stringify(trace) === JSON.stringify([\\n        \'sync 1\',\\n        \'sync 2\',\\n        \'micro promise 1\',\\n        \'micro promise 2\',\\n        \'macro timeout\'\\n      ]),\\n      \'Async execution trace failed\'\\n    );\\n    console.log(\'Final Execution Trace:\', trace);\\n    console.log(\'\\u2713 All Execution Order tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { runExecutionOrderQuiz, simulateOrder };\\n"\n      }\n    ],\n    [\n      13,\n      "feat: implement custom polyfill for Promise (resolving, rejecting, chaining)",\n      {\n        "days/day13-promise-polyfill/promise.js": "/**\\n * Custom Polyfill for Promise (Promises/A+ Spec Compliant Core)\\n */\\n\\nconst STATE = {\\n  PENDING: \'pending\',\\n  FULFILLED: \'fulfilled\',\\n  REJECTED: \'rejected\',\\n};\\n\\nclass MyPromise {\\n  constructor(executor) {\\n    if (typeof executor !== \'function\') {\\n      throw new TypeError(\'Promise resolver \' + executor + \' is not a function\');\\n    }\\n\\n    this.state = STATE.PENDING;\\n    this.value = undefined;\\n    this.handlers = [];\\n\\n    const resolve = (val) => {\\n      if (this.state !== STATE.PENDING) return;\\n\\n      // Unwrapping thenables / nested promises\\n      if (val && (typeof val === \'object\' || typeof val === \'function\') && typeof val.then === \'function\') {\\n        val.then(resolve, reject);\\n        return;\\n      }\\n\\n      this.state = STATE.FULFILLED;\\n      this.value = val;\\n      this._executeHandlers();\\n    };\\n\\n    const reject = (reason) => {\\n      if (this.state !== STATE.PENDING) return;\\n\\n      this.state = STATE.REJECTED;\\n      this.value = reason;\\n      this._executeHandlers();\\n    };\\n\\n    try {\\n      executor(resolve, reject);\\n    } catch (err) {\\n      reject(err);\\n    }\\n  }\\n\\n  _executeHandlers() {\\n    if (this.state === STATE.PENDING) return;\\n\\n    // Must be executed asynchronously via microtask\\n    queueMicrotask(() => {\\n      for (const { onFulfilled, onRejected, resolve, reject } of this.handlers) {\\n        try {\\n          if (this.state === STATE.FULFILLED) {\\n            if (typeof onFulfilled === \'function\') {\\n              resolve(onFulfilled(this.value));\\n            } else {\\n              resolve(this.value);\\n            }\\n          } else if (this.state === STATE.REJECTED) {\\n            if (typeof onRejected === \'function\') {\\n              resolve(onRejected(this.value));\\n            } else {\\n              reject(this.value);\\n            }\\n          }\\n        } catch (err) {\\n          reject(err);\\n        }\\n      }\\n      this.handlers = [];\\n    });\\n  }\\n\\n  then(onFulfilled, onRejected) {\\n    return new MyPromise((resolve, reject) => {\\n      this.handlers.push({ onFulfilled, onRejected, resolve, reject });\\n      this._executeHandlers();\\n    });\\n  }\\n\\n  catch(onRejected) {\\n    return this.then(null, onRejected);\\n  }\\n\\n  finally(callback) {\\n    return this.then(\\n      (val) => MyPromise.resolve(callback()).then(() => val),\\n      (err) => MyPromise.resolve(callback()).then(() => { throw err; })\\n    );\\n  }\\n\\n  static resolve(value) {\\n    if (value instanceof MyPromise) return value;\\n    return new MyPromise((res) => res(value));\\n  }\\n\\n  static reject(reason) {\\n    return new MyPromise((_, rej) => rej(reason));\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing MyPromise Polyfill ---\');\\n\\n  const p = new MyPromise((resolve) => {\\n    setTimeout(() => resolve(100), 20);\\n  });\\n\\n  p.then((val) => {\\n    console.assert(val === 100, \'Initial resolve value failed\');\\n    return val * 2;\\n  })\\n  .then((val2) => {\\n    console.assert(val2 === 200, \'Chaining value failed\');\\n    console.log(\'\\u2713 All MyPromise tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { MyPromise };\\n"\n      }\n    ],\n    [\n      14,\n      "feat: implement Promise.all, Promise.race, and Promise.allSettled from scratch",\n      {\n        "days/day14-promise-combinators/combinators.js": "/**\\n * Implementation of Promise Combinators from scratch:\\n * all, race, allSettled, any\\n */\\n\\nfunction promiseAll(iterable) {\\n  return new Promise((resolve, reject) => {\\n    const promises = Array.from(iterable);\\n    if (promises.length === 0) {\\n      return resolve([]);\\n    }\\n\\n    const results = new Array(promises.length);\\n    let completed = 0;\\n\\n    promises.forEach((item, index) => {\\n      Promise.resolve(item)\\n        .then((val) => {\\n          results[index] = val;\\n          completed++;\\n          if (completed === promises.length) {\\n            resolve(results);\\n          }\\n        })\\n        .catch(reject);\\n    });\\n  });\\n}\\n\\nfunction promiseRace(iterable) {\\n  return new Promise((resolve, reject) => {\\n    const promises = Array.from(iterable);\\n    promises.forEach((item) => {\\n      Promise.resolve(item).then(resolve).catch(reject);\\n    });\\n  });\\n}\\n\\nfunction promiseAllSettled(iterable) {\\n  return new Promise((resolve) => {\\n    const promises = Array.from(iterable);\\n    if (promises.length === 0) {\\n      return resolve([]);\\n    }\\n\\n    const results = new Array(promises.length);\\n    let completed = 0;\\n\\n    promises.forEach((item, index) => {\\n      Promise.resolve(item)\\n        .then((value) => {\\n          results[index] = { status: \'fulfilled\', value };\\n        })\\n        .catch((reason) => {\\n          results[index] = { status: \'rejected\', reason };\\n        })\\n        .finally(() => {\\n          completed++;\\n          if (completed === promises.length) {\\n            resolve(results);\\n          }\\n        });\\n    });\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Promise Combinators ---\');\\n\\n  const p1 = Promise.resolve(1);\\n  const p2 = 42;\\n  const p3 = new Promise((res) => setTimeout(() => res(\'slow\'), 30));\\n\\n  promiseAll([p1, p2, p3]).then((res) => {\\n    console.assert(JSON.stringify(res) === JSON.stringify([1, 42, \'slow\']), \'promiseAll failed\');\\n  });\\n\\n  const fast = new Promise((res) => setTimeout(() => res(\'fast\'), 10));\\n  const slow = new Promise((res) => setTimeout(() => res(\'slow\'), 50));\\n  promiseRace([fast, slow]).then((winner) => {\\n    console.assert(winner === \'fast\', \'promiseRace failed\');\\n  });\\n\\n  const rejected = Promise.reject(\'error\');\\n  promiseAllSettled([p1, rejected]).then((settled) => {\\n    console.assert(settled[0].status === \'fulfilled\' && settled[0].value === 1, \'allSettled fulfilled failed\');\\n    console.assert(settled[1].status === \'rejected\' && settled[1].reason === \'error\', \'allSettled rejected failed\');\\n    console.log(\'\\u2713 All Promise Combinators tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { promiseAll, promiseRace, promiseAllSettled };\\n"\n      }\n    ],\n    [\n      15,\n      "docs: add deep-dive notes on async/await vs Generators",\n      {\n        "docs/day15-async-generators.md": "# Deep Dive: async/await vs Generator Functions\\n\\n## 1. Generators as Coroutines\\nA **generator function** (`function*`) produces a `Generator` object that conforms to both the **Iterable** and **Iterator** protocols. It can pause execution via `yield` and resume via `.next()`, passing data bi-directionally.\\n\\n```javascript\\nfunction* counter() {\\n  const step = yield 1; // pause and return 1; resumes with step argument\\n  yield 1 + (step || 1);\\n}\\n\\nconst gen = counter();\\nconsole.log(gen.next());       // { value: 1, done: false }\\nconsole.log(gen.next(10));     // { value: 11, done: false }\\n```\\n\\n---\\n\\n## 2. Async/Await is Syntactic Sugar for Generator + Promise Runner\\nUnder the hood, `async/await` is equivalent to a generator driven by a promise runner (like the famous `co` library):\\n\\n```javascript\\n// Custom async runner simulating async/await using generators\\nfunction asyncRunner(generatorFn) {\\n  return function(...args) {\\n    const gen = generatorFn.apply(this, args);\\n\\n    return new Promise((resolve, reject) => {\\n      function step(key, arg) {\\n        let result;\\n        try {\\n          result = gen[key](arg);\\n        } catch (err) {\\n          return reject(err);\\n        }\\n\\n        const { value, done } = result;\\n        if (done) {\\n          return resolve(value);\\n        }\\n\\n        return Promise.resolve(value).then(\\n          (val) => step(\'next\', val),\\n          (err) => step(\'throw\', err)\\n        );\\n      }\\n\\n      step(\'next\');\\n    });\\n  };\\n}\\n```\\n\\n---\\n\\n## 3. Comparison Summary\\n\\n| Feature | `async / await` | `function* / yield` |\\n|:---|:---|:---|\\n| **Return Type** | Always returns a `Promise` | Returns a `Generator` Iterator |\\n| **Control Flow** | Asynchronous resolution only | Synchronous or asynchronous pause/resume |\\n| **Re-entrancy** | Resumed automatically by Microtask queue | Resumed manually via `.next()` |\\n| **State Streaming** | Single final resolution value | Stream of values over time (lazy pull) |\\n"\n      }\n    ],\n    [\n      16,\n      "feat: implement custom deep clone (deepCopy) utility handling circular references",\n      {\n        "days/day16-deep-clone/deep-clone.js": "/**\\n * Custom Deep Clone (deepCopy) Utility\\n * Handles circular references, Map, Set, Date, RegExp, and primitive types.\\n */\\n\\nfunction deepClone(value, hash = new WeakMap()) {\\n  // Primitives and functions\\n  if (value === null || typeof value !== \'object\') {\\n    return value;\\n  }\\n\\n  // Circular reference detection\\n  if (hash.has(value)) {\\n    return hash.get(value);\\n  }\\n\\n  // Dates\\n  if (value instanceof Date) {\\n    return new Date(value.getTime());\\n  }\\n\\n  // Regular Expressions\\n  if (value instanceof RegExp) {\\n    return new RegExp(value.source, value.flags);\\n  }\\n\\n  // Maps\\n  if (value instanceof Map) {\\n    const copyMap = new Map();\\n    hash.set(value, copyMap);\\n    value.forEach((val, key) => {\\n      copyMap.set(deepClone(key, hash), deepClone(val, hash));\\n    });\\n    return copyMap;\\n  }\\n\\n  // Sets\\n  if (value instanceof Set) {\\n    const copySet = new Set();\\n    hash.set(value, copySet);\\n    value.forEach((val) => {\\n      copySet.add(deepClone(val, hash));\\n    });\\n    return copySet;\\n  }\\n\\n  // Arrays and Plain Objects\\n  const isArray = Array.isArray(value);\\n  const copy = isArray ? [] : Object.create(Object.getPrototypeOf(value));\\n  hash.set(value, copy);\\n\\n  // Copy Symbols and regular keys\\n  const keys = [...Object.keys(value), ...Object.getOwnPropertySymbols(value)];\\n  for (const key of keys) {\\n    copy[key] = deepClone(value[key], hash);\\n  }\\n\\n  return copy;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing deepClone Utility ---\');\\n\\n  const original = {\\n    num: 42,\\n    str: \'hello\',\\n    date: new Date(\'2026-01-16\'),\\n    regex: /abc/gi,\\n    nested: { arr: [1, 2, { a: \'b\' }] },\\n    set: new Set([1, 2, 3]),\\n    map: new Map([[\'key\', { inner: \'val\' }]]),\\n  };\\n\\n  // Circular reference\\n  original.self = original;\\n\\n  const cloned = deepClone(original);\\n\\n  console.assert(cloned !== original, \'Cloned must have distinct reference\');\\n  console.assert(cloned.self === cloned, \'Circular reference preserved\');\\n  console.assert(cloned.nested.arr !== original.nested.arr, \'Nested array cloned\');\\n  console.assert(cloned.date.getTime() === original.date.getTime(), \'Date copied\');\\n  console.assert(cloned.map.get(\'key\').inner === \'val\', \'Map copied\');\\n  console.assert(cloned.map.get(\'key\') !== original.map.get(\'key\'), \'Map inner value deep cloned\');\\n\\n  console.log(\'\\u2713 All deepClone tests passed!\');\\n}\\n\\nmodule.exports = { deepClone };\\n"\n      }\n    ],\n    [\n      17,\n      "feat: implement object flattening function for deeply nested JSON objects",\n      {\n        "days/day17-flatten-object/flatten.js": "/**\\n * Object Flattening and Unflattening Utility for Deeply Nested Objects\\n */\\n\\nfunction flattenObject(obj, prefix = \'\', result = {}) {\\n  if (obj === null || typeof obj !== \'object\') {\\n    if (prefix) result[prefix] = obj;\\n    return result;\\n  }\\n\\n  for (const key of Object.keys(obj)) {\\n    const val = obj[key];\\n    const newKey = prefix ? prefix + \'.\' + key : key;\\n\\n    if (val !== null && typeof val === \'object\' && !(val instanceof Date) && !(val instanceof RegExp)) {\\n      flattenObject(val, newKey, result);\\n    } else {\\n      result[newKey] = val;\\n    }\\n  }\\n\\n  return result;\\n}\\n\\nfunction unflattenObject(flatObj) {\\n  const result = {};\\n\\n  for (const [keyPath, value] of Object.entries(flatObj)) {\\n    const keys = keyPath.split(\'.\');\\n    let current = result;\\n\\n    for (let i = 0; i < keys.length; i++) {\\n      const k = keys[i];\\n      if (i === keys.length - 1) {\\n        current[k] = value;\\n      } else {\\n        const nextKey = keys[i + 1];\\n        const isNextArray = !isNaN(Number(nextKey));\\n        if (!current[k]) {\\n          current[k] = isNextArray ? [] : {};\\n        }\\n        current = current[k];\\n      }\\n    }\\n  }\\n\\n  return result;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Object Flattening ---\');\\n\\n  const nested = {\\n    user: {\\n      profile: {\\n        name: \'Alex\',\\n        address: {\\n          city: \'San Francisco\',\\n          zip: \'94105\'\\n        }\\n      },\\n      tags: [\'engineer\', \'javascript\']\\n    }\\n  };\\n\\n  const flattened = flattenObject(nested);\\n  console.assert(flattened[\'user.profile.name\'] === \'Alex\', \'Name flatten failed\');\\n  console.assert(flattened[\'user.profile.address.city\'] === \'San Francisco\', \'City flatten failed\');\\n  console.assert(flattened[\'user.tags.0\'] === \'engineer\', \'Array index flatten failed\');\\n\\n  const unflattened = unflattenObject(flattened);\\n  console.assert(unflattened.user.profile.name === \'Alex\', \'Unflatten name failed\');\\n  console.assert(unflattened.user.tags[0] === \'engineer\', \'Unflatten array failed\');\\n\\n  console.log(\'\\u2713 All Object Flattening tests passed!\');\\n}\\n\\nmodule.exports = { flattenObject, unflattenObject };\\n"\n      }\n    ],\n    [\n      18,\n      "docs: explain Prototypal Inheritance and the Prototype Chain with code examples",\n      {\n        "docs/day18-prototypal-inheritance.md": "# Prototypal Inheritance and the Prototype Chain\\n\\n## 1. Prototype Delegation in JavaScript\\nUnlike class-based languages (like Java or C++) that instantiate distinct instances from blueprints, JavaScript objects are linked directly to other objects through an internal link (`[[Prototype]]`). When accessing a property on an object, JavaScript checks if the property exists on the object itself. If not, it searches the object\'s prototype, traversing up the **prototype chain**.\\n\\n```\\n  myArray -> Array.prototype -> Object.prototype -> null\\n```\\n\\n---\\n\\n## 2. `__proto__` vs `prototype`\\n- `prototype`: A property present on constructor functions (e.g., `Person.prototype`) used to establish the `[[Prototype]]` of instances created with `new Person()`.\\n- `__proto__`: The legacy accessor (or `Object.getPrototypeOf(obj)`) exposing an object\'s internal `[[Prototype]]`.\\n\\n---\\n\\n## 3. Pure Prototypal Inheritance with `Object.create()`\\n\\n```javascript\\nconst animal = {\\n  makeSound() {\\n    return this.sound || \'Silence\';\\n  },\\n  eat() {\\n    return \'Eating...\';\\n  }\\n};\\n\\n// Create dog delegating directly to animal\\nconst dog = Object.create(animal);\\ndog.sound = \'Woof!\';\\n\\nconsole.log(dog.makeSound()); // \'Woof!\' (found on dog)\\nconsole.log(dog.eat());       // \'Eating...\' (delegated to animal)\\nconsole.log(dog.hasOwnProperty(\'sound\')); // true\\nconsole.log(dog.hasOwnProperty(\'eat\'));   // false\\n```\\n\\n---\\n\\n## 4. Property Shadowing\\nIf an object defines a property with the same name as a property further up its prototype chain, the object\'s own property \\"shadows\\" (hides) the prototype property.\\n"\n      }\n    ],\n    [\n      19,\n      "feat: implement custom polyfill for the JavaScript \'call\', \'apply\', and \'bind\' methods",\n      {\n        "days/day19-call-apply-bind/call-apply-bind.js": "/**\\n * Custom Polyfills for call, apply, and bind\\n */\\n\\nFunction.prototype.myCall = function(thisArg, ...args) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  // Handle null/undefined thisArg\\n  thisArg = thisArg == null ? globalThis : Object(thisArg);\\n\\n  const fnSymbol = Symbol(\'fn\');\\n  thisArg[fnSymbol] = this;\\n\\n  const result = thisArg[fnSymbol](...args);\\n  delete thisArg[fnSymbol];\\n\\n  return result;\\n};\\n\\nFunction.prototype.myApply = function(thisArg, argsArray) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  thisArg = thisArg == null ? globalThis : Object(thisArg);\\n\\n  const fnSymbol = Symbol(\'fn\');\\n  thisArg[fnSymbol] = this;\\n\\n  let result;\\n  if (!argsArray) {\\n    result = thisArg[fnSymbol]();\\n  } else {\\n    result = thisArg[fnSymbol](...argsArray);\\n  }\\n\\n  delete thisArg[fnSymbol];\\n  return result;\\n};\\n\\nFunction.prototype.myBind = function(thisArg, ...boundArgs) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  const originalFn = this;\\n\\n  function boundFunction(...callArgs) {\\n    // If called as constructor with \'new\', \'this\' should be the new instance\\n    const isNew = this instanceof boundFunction;\\n    const context = isNew ? this : thisArg;\\n    return originalFn.apply(context, [...boundArgs, ...callArgs]);\\n  }\\n\\n  // Maintain prototype chain for \'new\'\\n  if (originalFn.prototype) {\\n    boundFunction.prototype = Object.create(originalFn.prototype);\\n    boundFunction.prototype.constructor = boundFunction;\\n  }\\n\\n  return boundFunction;\\n};\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing call, apply, and bind Polyfills ---\');\\n\\n  function greet(greeting, punctuation) {\\n    return greeting + \', \' + this.name + punctuation;\\n  }\\n\\n  const user = { name: \'Sarah\' };\\n\\n  console.assert(greet.myCall(user, \'Hello\', \'!\') === \'Hello, Sarah!\', \'myCall failed\');\\n  console.assert(greet.myApply(user, [\'Hi\', \'?\']) === \'Hi, Sarah?\', \'myApply failed\');\\n\\n  const bound = greet.myBind(user, \'Hey\');\\n  console.assert(bound(\'.\') === \'Hey, Sarah.\', \'myBind failed\');\\n\\n  // Test constructor with bound function\\n  function Person(age) {\\n    this.age = age;\\n  }\\n  const BoundPerson = Person.myBind({ dummy: true });\\n  const pInstance = new BoundPerson(25);\\n  console.assert(pInstance.age === 25, \'myBind constructor age failed\');\\n  console.assert(pInstance instanceof Person, \'myBind constructor instanceof failed\');\\n\\n  console.log(\'\\u2713 All call, apply, and bind tests passed!\');\\n}\\n\\nmodule.exports = {};\\n"\n      }\n    ],\n    [\n      20,\n      "feat: implement classical vs prototypal inheritance structures using ES6 classes",\n      {\n        "days/day20-es6-inheritance/inheritance.js": "/**\\n * Classical vs Prototypal Inheritance with ES5 and ES6 Classes\\n */\\n\\n// 1. ES5 Pseudo-Classical Pattern\\nfunction VehicleES5(make, model) {\\n  this.make = make;\\n  this.model = model;\\n}\\n\\nVehicleES5.prototype.getDetails = function() {\\n  return this.make + \' \' + this.model;\\n};\\n\\nfunction CarES5(make, model, doors) {\\n  VehicleES5.call(this, make, model);\\n  this.doors = doors;\\n}\\n\\nCarES5.prototype = Object.create(VehicleES5.prototype);\\nCarES5.prototype.constructor = CarES5;\\n\\nCarES5.prototype.getDetails = function() {\\n  return VehicleES5.prototype.getDetails.call(this) + \' with \' + this.doors + \' doors\';\\n};\\n\\n// 2. ES6 Modern Class Pattern\\nclass VehicleES6 {\\n  constructor(make, model) {\\n    this.make = make;\\n    this.model = model;\\n  }\\n\\n  getDetails() {\\n    return `${this.make} ${this.model}`;\\n  }\\n}\\n\\nclass CarES6 extends VehicleES6 {\\n  #vin; // Private field\\n\\n  constructor(make, model, doors, vin) {\\n    super(make, model);\\n    this.doors = doors;\\n    this.#vin = vin;\\n  }\\n\\n  getDetails() {\\n    return `${super.getDetails()} with ${this.doors} doors`;\\n  }\\n\\n  getVin() {\\n    return this.#vin;\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing ES5 vs ES6 Inheritance ---\');\\n\\n  const es5Car = new CarES5(\'Toyota\', \'Corolla\', 4);\\n  console.assert(es5Car.getDetails() === \'Toyota Corolla with 4 doors\', \'ES5 inheritance failed\');\\n  console.assert(es5Car instanceof VehicleES5, \'ES5 instanceof failed\');\\n\\n  const es6Car = new CarES6(\'Tesla\', \'Model 3\', 4, \'VIN12345\');\\n  console.assert(es6Car.getDetails() === \'Tesla Model 3 with 4 doors\', \'ES6 inheritance failed\');\\n  console.assert(es6Car instanceof VehicleES6, \'ES6 instanceof failed\');\\n  console.assert(es6Car.getVin() === \'VIN12345\', \'ES6 private field failed\');\\n\\n  console.log(\'\\u2713 All Inheritance structure tests passed!\');\\n}\\n\\nmodule.exports = { VehicleES5, CarES5, VehicleES6, CarES6 };\\n"\n      }\n    ],\n    [\n      21,\n      "docs: document \'this\' keyword behavior across global, object, arrow functions, and event listeners",\n      {\n        "docs/day21-this-keyword.md": "# JavaScript \'this\' Keyword Behavior Comprehensive Guide\\n\\n## The 4 Rules of \'this\' Binding\\n\\nThe value of `this` in JavaScript is NOT determined by where a function is declared, but **how the function is invoked**.\\n\\n---\\n\\n### 1. Default Binding (Standalone Function Call)\\nWhen a function is called without any context object:\\n- In **non-strict mode**: `this` points to the global object (`window` in browsers, `global` in Node.js).\\n- In **strict mode (`\'use strict\'`)**: `this` is `undefined`.\\n\\n```javascript\\nfunction show() {\\n  \'use strict\';\\n  return this; // undefined\\n}\\n```\\n\\n---\\n\\n### 2. Implicit Binding (Object Method Call)\\nWhen a function is called with a preceding context object (`obj.fn()`), `this` binds to that object:\\n\\n```javascript\\nconst user = {\\n  name: \'Marcus\',\\n  getName() { return this.name; }\\n};\\nuser.getName(); // \'Marcus\'\\n\\n// Pitfall: Losing implicit binding!\\nconst detached = user.getName;\\ndetached(); // undefined (or error in strict mode)\\n```\\n\\n---\\n\\n### 3. Explicit Binding (`call`, `apply`, `bind`)\\nForces a function to execute with an explicitly chosen `thisArg`:\\n\\n```javascript\\nfunction printRole() { return this.role; }\\nconst admin = { role: \'Administrator\' };\\nprintRole.call(admin); // \'Administrator\'\\n```\\n\\n---\\n\\n### 4. `new` Binding (Constructor Call)\\nWhen invoked with `new Constructor()`:\\n1. A brand new empty object is created.\\n2. Its `[[Prototype]]` is set to `Constructor.prototype`.\\n3. `this` is bound to the new object.\\n4. Returns the object (unless constructor explicitly returns another object).\\n\\n---\\n\\n### 5. Arrow Functions: Lexical `this`\\nArrow functions do **NOT** have their own `this`. They inherit `this` lexically from the enclosing execution context at definition time:\\n\\n```javascript\\nconst timer = {\\n  seconds: 0,\\n  start() {\\n    setTimeout(() => {\\n      this.seconds++; // \'this\' reliably refers to timer object\\n    }, 100);\\n  }\\n};\\n```\\n"\n      }\n    ],\n    [\n      22,\n      "feat: implement a pub/sub (Event Emitter) design pattern in JavaScript",\n      {\n        "days/day22-pub-sub/event-emitter.js": "/**\\n * Pub/Sub (Event Emitter) Pattern in JavaScript\\n */\\n\\nclass EventEmitter {\\n  constructor() {\\n    this.events = new Map();\\n  }\\n\\n  on(eventName, listener) {\\n    if (typeof listener !== \'function\') {\\n      throw new TypeError(\'Listener must be a function\');\\n    }\\n    if (!this.events.has(eventName)) {\\n      this.events.set(eventName, []);\\n    }\\n    this.events.get(eventName).push(listener);\\n\\n    // Unsubscribe handle\\n    return () => this.off(eventName, listener);\\n  }\\n\\n  once(eventName, listener) {\\n    if (typeof listener !== \'function\') {\\n      throw new TypeError(\'Listener must be a function\');\\n    }\\n    const wrapper = (...args) => {\\n      this.off(eventName, wrapper);\\n      listener.apply(this, args);\\n    };\\n    wrapper.originalListener = listener;\\n    return this.on(eventName, wrapper);\\n  }\\n\\n  off(eventName, listener) {\\n    if (!this.events.has(eventName)) return;\\n\\n    const listeners = this.events.get(eventName).filter(\\n      (l) => l !== listener && l.originalListener !== listener\\n    );\\n\\n    if (listeners.length === 0) {\\n      this.events.delete(eventName);\\n    } else {\\n      this.events.set(eventName, listeners);\\n    }\\n  }\\n\\n  emit(eventName, ...args) {\\n    if (!this.events.has(eventName)) return false;\\n\\n    // Clone listeners to avoid mutations during iteration\\n    const listeners = [...this.events.get(eventName)];\\n    for (const listener of listeners) {\\n      listener.apply(this, args);\\n    }\\n    return true;\\n  }\\n\\n  listenerCount(eventName) {\\n    return this.events.has(eventName) ? this.events.get(eventName).length : 0;\\n  }\\n\\n  removeAllListeners(eventName) {\\n    if (eventName) {\\n      this.events.delete(eventName);\\n    } else {\\n      this.events.clear();\\n    }\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing EventEmitter ---\');\\n\\n  const emitter = new EventEmitter();\\n  let callCount = 0;\\n  let receivedData = null;\\n\\n  const unsubscribe = emitter.on(\'data\', (data) => {\\n    callCount++;\\n    receivedData = data;\\n  });\\n\\n  emitter.emit(\'data\', { id: 1 });\\n  console.assert(callCount === 1 && receivedData.id === 1, \'Event emit failed\');\\n\\n  // Once test\\n  let onceCount = 0;\\n  emitter.once(\'single\', () => onceCount++);\\n  emitter.emit(\'single\');\\n  emitter.emit(\'single\');\\n  console.assert(onceCount === 1, \'Once listener failed\');\\n\\n  // Unsubscribe test\\n  unsubscribe();\\n  emitter.emit(\'data\', { id: 2 });\\n  console.assert(callCount === 1, \'Unsubscribe failed\');\\n\\n  console.log(\'\\u2713 All EventEmitter tests passed!\');\\n}\\n\\nmodule.exports = { EventEmitter };\\n"\n      }\n    ],\n    [\n      23,\n      "feat: build a currying utility function with placeholder support",\n      {\n        "days/day23-curry/curry.js": "/**\\n * Currying Utility with Placeholder Support\\n */\\n\\nconst _ = Symbol(\'curry_placeholder\');\\n\\nfunction curry(fn, arity = fn.length) {\\n  return function curried(...args) {\\n    // Count real (non-placeholder) arguments\\n    const complete = args.length >= arity &&\\n      args.slice(0, arity).every((arg) => arg !== _);\\n\\n    if (complete) {\\n      return fn.apply(this, args.slice(0, arity));\\n    }\\n\\n    return function(...nextArgs) {\\n      // Merge previous args and nextArgs, filling placeholders\\n      const combinedArgs = [];\\n      let nextIndex = 0;\\n\\n      for (let i = 0; i < args.length; i++) {\\n        if (args[i] === _ && nextIndex < nextArgs.length) {\\n          combinedArgs.push(nextArgs[nextIndex++]);\\n        } else {\\n          combinedArgs.push(args[i]);\\n        }\\n      }\\n\\n      while (nextIndex < nextArgs.length) {\\n        combinedArgs.push(nextArgs[nextIndex++]);\\n      }\\n\\n      return curried.apply(this, combinedArgs);\\n    };\\n  };\\n}\\n\\ncurry._ = _;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Curry with Placeholder ---\');\\n\\n  function calculate(a, b, c) {\\n    return a + (b * c);\\n  }\\n\\n  const curriedCalc = curry(calculate);\\n\\n  console.assert(curriedCalc(2)(3)(4) === 14, \'Standard curry failed\');\\n  console.assert(curriedCalc(2, 3, 4) === 14, \'Direct call failed\');\\n\\n  // Placeholder tests\\n  const withPlaceholder = curriedCalc(curry._, 3)(2)(4);\\n  console.assert(withPlaceholder === 14, \'Single placeholder failed\');\\n\\n  const multiPlaceholder = curriedCalc(curry._, curry._, 5)(10, 2);\\n  console.assert(multiPlaceholder === 20, \'Multi placeholder failed\');\\n\\n  console.log(\'\\u2713 All Curry tests passed!\');\\n}\\n\\nmodule.exports = { curry, _ };\\n"\n      }\n    ],\n    [\n      24,\n      "feat: implement function composition and piping helper utilities",\n      {\n        "days/day24-compose-pipe/compose-pipe.js": "/**\\n * Function Composition (Right-to-Left) and Piping (Left-to-Right)\\n */\\n\\nfunction compose(...fns) {\\n  if (fns.length === 0) return (arg) => arg;\\n  if (fns.length === 1) return fns[0];\\n\\n  return function(initialValue) {\\n    return fns.reduceRight((acc, fn) => fn(acc), initialValue);\\n  };\\n}\\n\\nfunction pipe(...fns) {\\n  if (fns.length === 0) return (arg) => arg;\\n  if (fns.length === 1) return fns[0];\\n\\n  return function(initialValue) {\\n    return fns.reduce((acc, fn) => fn(acc), initialValue);\\n  };\\n}\\n\\n// Asynchronous pipe for Promise chains\\nfunction pipeAsync(...fns) {\\n  return function(initialValue) {\\n    return fns.reduce(\\n      (promiseChain, fn) => promiseChain.then(fn),\\n      Promise.resolve(initialValue)\\n    );\\n  };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Compose and Pipe ---\');\\n\\n  const add5 = (x) => x + 5;\\n  const multiply2 = (x) => x * 2;\\n  const square = (x) => x * x;\\n\\n  // compose executes right-to-left: square(multiply2(add5(2))) -> (2+5)=7 -> 7*2=14 -> 14^2=196\\n  const composed = compose(square, multiply2, add5);\\n  console.assert(composed(2) === 196, \'Compose failed\');\\n\\n  // pipe executes left-to-right: square(2)=4 -> add5(4)=9 -> multiply2(9)=18\\n  const piped = pipe(square, add5, multiply2);\\n  console.assert(piped(2) === 18, \'Pipe failed\');\\n\\n  // Async pipe\\n  const asyncAdd = async (x) => x + 10;\\n  const asyncDouble = async (x) => x * 2;\\n  pipeAsync(asyncAdd, asyncDouble)(5).then((val) => {\\n    console.assert(val === 30, \'Async pipe failed\');\\n    console.log(\'\\u2713 All Compose & Pipe tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { compose, pipe, pipeAsync };\\n"\n      }\n    ],\n    [\n      25,\n      "docs: break down Garbage Collection mechanisms (Mark-and-Sweep, memory leaks)",\n      {\n        "docs/day25-garbage-collection.md": "# V8 Garbage Collection & JavaScript Memory Leaks\\n\\n## 1. V8 Memory Architecture\\nV8 allocates memory into two main generation spaces:\\n1. **Young Generation (New Space)**:\\n   - Objects are initially allocated here.\\n   - Extremely fast allocation and garbage collection using the **Scavenge algorithm (Cheney\'s copying algorithm)**.\\n   - Split into two semi-spaces: `From-space` and `To-space`.\\n   - Objects surviving two scavenges are promoted to the Old Space.\\n2. **Old Generation (Old Space)**:\\n   - Holds long-lived objects.\\n   - Collected using the **Mark-Sweep-Compact** algorithm.\\n\\n---\\n\\n## 2. Mark-and-Sweep Algorithm\\n1. **Marking**: Starts from root nodes (Global object, active Call Stack local variables, DOM roots) and recursively traverses all references, marking reachable objects as active.\\n2. **Sweeping**: Scans the heap and reclaims memory from unmarked (unreachable) objects.\\n3. **Compacting**: Shifts surviving objects together to eliminate heap memory fragmentation.\\n\\n---\\n\\n## 3. Common Memory Leaks in JavaScript\\n\\n### Leak 1: Accidental Global Variables\\n```javascript\\nfunction leak() {\\n  leakedData = new Array(1000000); // Missing var/let/const attaches to window/global\\n}\\n```\\n\\n### Leak 2: Forgotten Timers / Callbacks\\n```javascript\\nconst heavyObject = { data: \'heavy\' };\\nsetInterval(() => {\\n  // Retains heavyObject in closure scope indefinitely\\n  console.log(heavyObject.data);\\n}, 1000);\\n```\\n\\n### Leak 3: Detached DOM Trees\\nHolding references to removed DOM elements in JavaScript memory prevents GC from clearing the DOM node and all its children.\\n\\n### Leak 4: Closures Retaining Unused Scope Variables\\n```javascript\\nlet unusedClosure = function() {\\n  const hugeData = new Array(1000000);\\n  return function() {\\n    // Even if unused, outer lexical environment may hold hugeData\\n  };\\n};\\n```\\n"\n      }\n    ],\n    [\n      26,\n      "feat: implement a simple Proxy and Reflect API data-binding mechanism",\n      {\n        "days/day26-proxy-binding/reactive.js": "/**\\n * Simple Reactive Data-Binding Store using Proxy & Reflect\\n */\\n\\nlet activeEffect = null;\\n\\nclass ReactiveStore {\\n  constructor(initialState = {}) {\\n    this.targetMap = new WeakMap();\\n    this.state = this._reactive(initialState);\\n  }\\n\\n  _track(target, key) {\\n    if (!activeEffect) return;\\n\\n    let depsMap = this.targetMap.get(target);\\n    if (!depsMap) {\\n      depsMap = new Map();\\n      this.targetMap.set(target, depsMap);\\n    }\\n\\n    let dep = depsMap.get(key);\\n    if (!dep) {\\n      dep = new Set();\\n      depsMap.set(key, dep);\\n    }\\n\\n    dep.add(activeEffect);\\n  }\\n\\n  _trigger(target, key) {\\n    const depsMap = this.targetMap.get(target);\\n    if (!depsMap) return;\\n\\n    const dep = depsMap.get(key);\\n    if (dep) {\\n      dep.forEach((effect) => effect());\\n    }\\n  }\\n\\n  _reactive(target) {\\n    if (target === null || typeof target !== \'object\') {\\n      return target;\\n    }\\n\\n    const self = this;\\n    return new Proxy(target, {\\n      get(obj, key, receiver) {\\n        const result = Reflect.get(obj, key, receiver);\\n        self._track(obj, key);\\n        return typeof result === \'object\' && result !== null\\n          ? self._reactive(result)\\n          : result;\\n      },\\n      set(obj, key, value, receiver) {\\n        const oldValue = obj[key];\\n        const success = Reflect.set(obj, key, value, receiver);\\n        if (oldValue !== value) {\\n          self._trigger(obj, key);\\n        }\\n        return success;\\n      }\\n    });\\n  }\\n\\n  watch(effectFn) {\\n    activeEffect = effectFn;\\n    effectFn();\\n    activeEffect = null;\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Proxy Data Binding ---\');\\n\\n  const store = new ReactiveStore({ count: 0, text: \'hello\' });\\n  let watchedValue = 0;\\n\\n  store.watch(() => {\\n    watchedValue = store.state.count * 2;\\n  });\\n\\n  console.assert(watchedValue === 0, \'Initial effect execution failed\');\\n\\n  store.state.count = 5;\\n  console.assert(watchedValue === 10, \'Reactive trigger failed\');\\n\\n  store.state.count = 12;\\n  console.assert(watchedValue === 24, \'Secondary reactive trigger failed\');\\n\\n  console.log(\'\\u2713 All Proxy Data Binding tests passed!\');\\n}\\n\\nmodule.exports = { ReactiveStore };\\n"\n      }\n    ],\n    [\n      27,\n      "feat: implement custom lazy loading utility for heavy operations",\n      {\n        "days/day27-lazy-loader/lazy.js": "/**\\n * Custom Lazy Loading Utilities for Heavy Computations and Resources\\n */\\n\\n// 1. Lazy evaluation wrapper (thunk with memoized return)\\nfunction lazy(fn) {\\n  let evaluated = false;\\n  let cachedResult;\\n\\n  return function() {\\n    if (!evaluated) {\\n      cachedResult = fn.apply(this, arguments);\\n      evaluated = true;\\n    }\\n    return cachedResult;\\n  };\\n}\\n\\n// 2. Define lazy property on an object that evaluates once upon first property access\\nfunction defineLazyProperty(target, propertyName, getterFn) {\\n  Object.defineProperty(target, propertyName, {\\n    configurable: true,\\n    enumerable: true,\\n    get() {\\n      const value = getterFn.call(this);\\n      // Replace accessor with concrete value to avoid future getter overhead\\n      Object.defineProperty(this, propertyName, {\\n        value,\\n        writable: true,\\n        configurable: true,\\n        enumerable: true,\\n      });\\n      return value;\\n    }\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Lazy Loading Utilities ---\');\\n\\n  let computationCount = 0;\\n  const heavyCalculation = lazy(() => {\\n    computationCount++;\\n    return 42 * 100;\\n  });\\n\\n  console.assert(computationCount === 0, \'Should not evaluate before call\');\\n  console.assert(heavyCalculation() === 4200, \'Calculation mismatch\');\\n  console.assert(computationCount === 1, \'Evaluated once\');\\n  console.assert(heavyCalculation() === 4200, \'Second call mismatch\');\\n  console.assert(computationCount === 1, \'Should not re-evaluate on subsequent calls\');\\n\\n  // Test lazy property\\n  const service = {};\\n  let propComputeCount = 0;\\n  defineLazyProperty(service, \'databaseConnection\', () => {\\n    propComputeCount++;\\n    return { connected: true, id: \'db-101\' };\\n  });\\n\\n  console.assert(propComputeCount === 0, \'Property should not compute eagerly\');\\n  console.assert(service.databaseConnection.connected === true, \'Property access failed\');\\n  console.assert(propComputeCount === 1, \'Property should compute once\');\\n  console.assert(service.databaseConnection.id === \'db-101\', \'Cached property access failed\');\\n  console.assert(propComputeCount === 1, \'Property should remain cached\');\\n\\n  console.log(\'\\u2713 All Lazy Loader tests passed!\');\\n}\\n\\nmodule.exports = { lazy, defineLazyProperty };\\n"\n      }\n    ],\n    [\n      28,\n      "docs: outline structural differences between WeakMap, WeakSet, Map, and Set",\n      {\n        "docs/day28-weakmap-weakset-map-set.md": "# WeakMap, WeakSet, Map, and Set in JavaScript\\n\\n## 1. Structural Comparison Matrix\\n\\n| Characteristic | `Map` | `WeakMap` | `Set` | `WeakSet` |\\n|:---|:---|:---|:---|:---|\\n| **Key/Value Types** | Any primitive or object | **Keys must be objects** (or registered symbols) | Any primitive or object | **Values must be objects** |\\n| **Garbage Collection** | Prevents GC of keys/values | **Weak references to keys** (does not prevent GC) | Prevents GC of values | **Weak references to values** |\\n| **Iterable?** | Yes (`.keys()`, `.values()`, `for..of`) | **NO** (non-enumerable) | Yes | **NO** |\\n| **`.size` property?** | Yes | **NO** | Yes | **NO** |\\n| **`.clear()` method?** | Yes | **NO** | Yes | **NO** |\\n\\n---\\n\\n## 2. Why Weak Collections Cannot Be Iterated\\nBecause keys in `WeakMap` and values in `WeakSet` are weakly held, they can be collected by the JavaScript engine\'s garbage collector non-deterministically at any moment. Exposing iteration or size would create non-deterministic race conditions based on when the engine\'s GC executes.\\n\\n---\\n\\n## 3. Practical Real-World Use Cases\\n\\n### WeakMap: Storing DOM Element Metadata\\n```javascript\\nconst clickCounts = new WeakMap();\\n\\nfunction trackButtonClick(button) {\\n  const count = clickCounts.get(button) || 0;\\n  clickCounts.set(button, count + 1);\\n}\\n// When \'button\' is removed from the DOM and has no references,\\n// its entry in clickCounts is automatically garbage-collected!\\n```\\n\\n### WeakSet: Tagging / Circular Reference Checking\\n```javascript\\nconst visitedObjects = new WeakSet();\\n\\nfunction markVisited(obj) {\\n  if (visitedObjects.has(obj)) {\\n    throw new Error(\'Circular structure detected\');\\n  }\\n  visitedObjects.add(obj);\\n}\\n```\\n"\n      }\n    ],\n    [\n      29,\n      "feat: solve common JS array/string algorithmic puzzle questions (e.g., sliding window)",\n      {\n        "days/day29-algorithms/puzzles.js": "/**\\n * Common JavaScript Algorithmic Puzzles:\\n * 1. Longest Substring Without Repeating Characters (Sliding Window)\\n * 2. Two Sum (Hash Map Lookup)\\n * 3. Maximum Subarray Sum (Kadane\'s Algorithm)\\n */\\n\\n// 1. Longest Substring Without Repeating Characters - O(n)\\nfunction lengthOfLongestSubstring(s) {\\n  const charMap = new Map();\\n  let maxLength = 0;\\n  let windowStart = 0;\\n\\n  for (let windowEnd = 0; windowEnd < s.length; windowEnd++) {\\n    const rightChar = s[windowEnd];\\n    if (charMap.has(rightChar)) {\\n      windowStart = Math.max(windowStart, charMap.get(rightChar) + 1);\\n    }\\n    charMap.set(rightChar, windowEnd);\\n    maxLength = Math.max(maxLength, windowEnd - windowStart + 1);\\n  }\\n\\n  return maxLength;\\n}\\n\\n// 2. Two Sum - O(n)\\nfunction twoSum(nums, target) {\\n  const seen = new Map();\\n  for (let i = 0; i < nums.length; i++) {\\n    const complement = target - nums[i];\\n    if (seen.has(complement)) {\\n      return [seen.get(complement), i];\\n    }\\n    seen.set(nums[i], i);\\n  }\\n  return [];\\n}\\n\\n// 3. Maximum Subarray Sum (Kadane\'s) - O(n)\\nfunction maxSubArray(nums) {\\n  if (nums.length === 0) return 0;\\n  let currentMax = nums[0];\\n  let globalMax = nums[0];\\n\\n  for (let i = 1; i < nums.length; i++) {\\n    currentMax = Math.max(nums[i], currentMax + nums[i]);\\n    globalMax = Math.max(globalMax, currentMax);\\n  }\\n\\n  return globalMax;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Algorithmic Puzzles ---\');\\n\\n  console.assert(lengthOfLongestSubstring(\'abcabcbb\') === 3, \'Longest substring 1 failed\');\\n  console.assert(lengthOfLongestSubstring(\'bbbbb\') === 1, \'Longest substring 2 failed\');\\n  console.assert(lengthOfLongestSubstring(\'pwwkew\') === 3, \'Longest substring 3 failed\');\\n\\n  const sumIndices = twoSum([2, 7, 11, 15], 9);\\n  console.assert(sumIndices[0] === 0 && sumIndices[1] === 1, \'Two Sum failed\');\\n\\n  const maxSub = maxSubArray([-2, 1, -3, 4, -1, 2, 1, -5, 4]);\\n  console.assert(maxSub === 6, \'Max subarray failed\'); // [4, -1, 2, 1]\\n\\n  console.log(\'\\u2713 All Algorithmic Puzzle tests passed!\');\\n}\\n\\nmodule.exports = { lengthOfLongestSubstring, twoSum, maxSubArray };\\n"\n      }\n    ]\n  ],\n  "day30_files": {\n    "src/index.js": "/**\\n * 31-Day JavaScript Mastery Series - Master Barrel Export\\n */\\n\\nmodule.exports = {\\n  // Polyfills\\n  ...require(\'./polyfills/map\'),\\n  ...require(\'./polyfills/filter\'),\\n  ...require(\'./polyfills/reduce\'),\\n  ...require(\'./polyfills/promise\'),\\n\\n  // Async\\n  ...require(\'./async/debounce\'),\\n  ...require(\'./async/throttle\'),\\n  ...require(\'./async/promise-combinators\'),\\n\\n  // Patterns\\n  ...require(\'./patterns/closure-privacy\'),\\n  ...require(\'./patterns/event-emitter\'),\\n  ...require(\'./patterns/reactive\'),\\n\\n  // Utilities\\n  ...require(\'./utils/memoize\'),\\n  ...require(\'./utils/deep-clone\'),\\n  ...require(\'./utils/flatten\'),\\n  ...require(\'./utils/curry\'),\\n  ...require(\'./utils/compose-pipe\'),\\n  ...require(\'./utils/lazy\'),\\n\\n  // Puzzles\\n  ...require(\'./puzzles/algorithms\'),\\n};\\n",\n    "src/async/debounce.js": "/**\\n * Custom Debounce Implementation with Leading and Trailing Options\\n */\\n\\nfunction debounce(fn, wait = 0, options = {}) {\\n  let timerId = null;\\n  let lastArgs = null;\\n  let lastThis = null;\\n  let result;\\n\\n  const leading = Boolean(options.leading);\\n  const trailing = \'trailing\' in options ? Boolean(options.trailing) : true;\\n\\n  function invoke(time) {\\n    const args = lastArgs;\\n    const thisArg = lastThis;\\n    lastArgs = null;\\n    lastThis = null;\\n    result = fn.apply(thisArg, args);\\n    return result;\\n  }\\n\\n  function debounced(...args) {\\n    lastArgs = args;\\n    lastThis = this;\\n\\n    const isInvokingLeading = leading && !timerId;\\n\\n    if (timerId) {\\n      clearTimeout(timerId);\\n    }\\n\\n    timerId = setTimeout(() => {\\n      timerId = null;\\n      if (trailing && !isInvokingLeading) {\\n        invoke();\\n      }\\n    }, wait);\\n\\n    if (isInvokingLeading) {\\n      return invoke();\\n    }\\n\\n    return result;\\n  }\\n\\n  debounced.cancel = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n    }\\n    lastArgs = null;\\n    lastThis = null;\\n  };\\n\\n  debounced.flush = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n      return invoke();\\n    }\\n    return result;\\n  };\\n\\n  debounced.pending = () => Boolean(timerId);\\n\\n  return debounced;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Debounce Function ---\');\\n\\n  let callCount = 0;\\n  const increment = debounce(() => {\\n    callCount++;\\n  }, 50);\\n\\n  increment();\\n  increment();\\n  increment();\\n  console.assert(callCount === 0, \'Should not have fired synchronously\');\\n\\n  setTimeout(() => {\\n    console.assert(callCount === 1, \'Should fire exactly once after wait\');\\n\\n    // Test leading option\\n    let leadingCalls = 0;\\n    const leadingFn = debounce(() => leadingCalls++, 50, { leading: true, trailing: false });\\n    leadingFn();\\n    leadingFn();\\n    console.assert(leadingCalls === 1, \'Leading call must fire immediately\');\\n\\n    console.log(\'\\u2713 All Debounce tests passed!\');\\n  }, 100);\\n}\\n\\nmodule.exports = { debounce };\\n",\n    "src/async/execution-order.js": "/**\\n * Tracing Asynchronous Execution Order: Promises, Microtasks, and Macrotasks\\n */\\n\\nfunction runExecutionOrderQuiz() {\\n  const output = [];\\n\\n  output.push(\'1: Synchronous Start\');\\n\\n  setTimeout(() => {\\n    output.push(\'2: setTimeout 0ms\');\\n  }, 0);\\n\\n  Promise.resolve()\\n    .then(() => {\\n      output.push(\'3: Microtask 1 (Promise)\');\\n      return \'Microtask 2 Chained\';\\n    })\\n    .then((val) => {\\n      output.push(\'4: \' + val);\\n    });\\n\\n  queueMicrotask(() => {\\n    output.push(\'5: queueMicrotask\');\\n  });\\n\\n  output.push(\'6: Synchronous End\');\\n\\n  return output;\\n}\\n\\n// Trace simulation helper for verification\\nfunction simulateOrder() {\\n  return new Promise((resolve) => {\\n    const trace = [];\\n\\n    trace.push(\'sync 1\');\\n\\n    setTimeout(() => {\\n      trace.push(\'macro timeout\');\\n      resolve(trace);\\n    }, 0);\\n\\n    Promise.resolve().then(() => {\\n      trace.push(\'micro promise 1\');\\n    }).then(() => {\\n      trace.push(\'micro promise 2\');\\n    });\\n\\n    trace.push(\'sync 2\');\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Asynchronous Execution Order ---\');\\n\\n  const syncLog = runExecutionOrderQuiz();\\n  console.assert(syncLog[0] === \'1: Synchronous Start\', \'Start order failed\');\\n  console.assert(syncLog[1] === \'6: Synchronous End\', \'Sync end order failed\');\\n\\n  simulateOrder().then((trace) => {\\n    console.assert(\\n      JSON.stringify(trace) === JSON.stringify([\\n        \'sync 1\',\\n        \'sync 2\',\\n        \'micro promise 1\',\\n        \'micro promise 2\',\\n        \'macro timeout\'\\n      ]),\\n      \'Async execution trace failed\'\\n    );\\n    console.log(\'Final Execution Trace:\', trace);\\n    console.log(\'\\u2713 All Execution Order tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { runExecutionOrderQuiz, simulateOrder };\\n",\n    "src/async/promise-combinators.js": "/**\\n * Implementation of Promise Combinators from scratch:\\n * all, race, allSettled, any\\n */\\n\\nfunction promiseAll(iterable) {\\n  return new Promise((resolve, reject) => {\\n    const promises = Array.from(iterable);\\n    if (promises.length === 0) {\\n      return resolve([]);\\n    }\\n\\n    const results = new Array(promises.length);\\n    let completed = 0;\\n\\n    promises.forEach((item, index) => {\\n      Promise.resolve(item)\\n        .then((val) => {\\n          results[index] = val;\\n          completed++;\\n          if (completed === promises.length) {\\n            resolve(results);\\n          }\\n        })\\n        .catch(reject);\\n    });\\n  });\\n}\\n\\nfunction promiseRace(iterable) {\\n  return new Promise((resolve, reject) => {\\n    const promises = Array.from(iterable);\\n    promises.forEach((item) => {\\n      Promise.resolve(item).then(resolve).catch(reject);\\n    });\\n  });\\n}\\n\\nfunction promiseAllSettled(iterable) {\\n  return new Promise((resolve) => {\\n    const promises = Array.from(iterable);\\n    if (promises.length === 0) {\\n      return resolve([]);\\n    }\\n\\n    const results = new Array(promises.length);\\n    let completed = 0;\\n\\n    promises.forEach((item, index) => {\\n      Promise.resolve(item)\\n        .then((value) => {\\n          results[index] = { status: \'fulfilled\', value };\\n        })\\n        .catch((reason) => {\\n          results[index] = { status: \'rejected\', reason };\\n        })\\n        .finally(() => {\\n          completed++;\\n          if (completed === promises.length) {\\n            resolve(results);\\n          }\\n        });\\n    });\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Promise Combinators ---\');\\n\\n  const p1 = Promise.resolve(1);\\n  const p2 = 42;\\n  const p3 = new Promise((res) => setTimeout(() => res(\'slow\'), 30));\\n\\n  promiseAll([p1, p2, p3]).then((res) => {\\n    console.assert(JSON.stringify(res) === JSON.stringify([1, 42, \'slow\']), \'promiseAll failed\');\\n  });\\n\\n  const fast = new Promise((res) => setTimeout(() => res(\'fast\'), 10));\\n  const slow = new Promise((res) => setTimeout(() => res(\'slow\'), 50));\\n  promiseRace([fast, slow]).then((winner) => {\\n    console.assert(winner === \'fast\', \'promiseRace failed\');\\n  });\\n\\n  const rejected = Promise.reject(\'error\');\\n  promiseAllSettled([p1, rejected]).then((settled) => {\\n    console.assert(settled[0].status === \'fulfilled\' && settled[0].value === 1, \'allSettled fulfilled failed\');\\n    console.assert(settled[1].status === \'rejected\' && settled[1].reason === \'error\', \'allSettled rejected failed\');\\n    console.log(\'\\u2713 All Promise Combinators tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { promiseAll, promiseRace, promiseAllSettled };\\n",\n    "src/async/throttle.js": "/**\\n * Custom Throttle Function\\n * Combines timestamp and setTimeout approaches to guarantee leading and trailing execution control.\\n */\\n\\nfunction throttle(fn, wait, options = {}) {\\n  let timerId = null;\\n  let lastExecTime = 0;\\n  let lastArgs = null;\\n  let lastThis = null;\\n\\n  const leading = \'leading\' in options ? Boolean(options.leading) : true;\\n  const trailing = \'trailing\' in options ? Boolean(options.trailing) : true;\\n\\n  function executeTrailing() {\\n    lastExecTime = leading ? Date.now() : 0;\\n    timerId = null;\\n    fn.apply(lastThis, lastArgs);\\n    lastArgs = null;\\n    lastThis = null;\\n  }\\n\\n  function throttled(...args) {\\n    const now = Date.now();\\n\\n    if (!lastExecTime && !leading) {\\n      lastExecTime = now;\\n    }\\n\\n    const remaining = wait - (now - lastExecTime);\\n\\n    lastArgs = args;\\n    lastThis = this;\\n\\n    if (remaining <= 0 || remaining > wait) {\\n      if (timerId) {\\n        clearTimeout(timerId);\\n        timerId = null;\\n      }\\n      lastExecTime = now;\\n      fn.apply(lastThis, lastArgs);\\n      lastArgs = null;\\n      lastThis = null;\\n    } else if (!timerId && trailing) {\\n      timerId = setTimeout(executeTrailing, remaining);\\n    }\\n  }\\n\\n  throttled.cancel = () => {\\n    if (timerId) {\\n      clearTimeout(timerId);\\n      timerId = null;\\n    }\\n    lastExecTime = 0;\\n    lastArgs = null;\\n    lastThis = null;\\n  };\\n\\n  return throttled;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Throttle Function ---\');\\n\\n  let executionCounter = 0;\\n  const throttledFn = throttle(() => {\\n    executionCounter++;\\n  }, 50);\\n\\n  throttledFn(); // immediate execution (leading)\\n  throttledFn(); // ignored\\n  throttledFn(); // ignored\\n  console.assert(executionCounter === 1, \'Leading execution must occur immediately\');\\n\\n  setTimeout(() => {\\n    // After 70ms, trailing call should have fired\\n    console.assert(executionCounter === 2, \'Trailing execution should have fired\');\\n    console.log(\'\\u2713 All Throttle tests passed!\');\\n  }, 100);\\n}\\n\\nmodule.exports = { throttle };\\n",\n    "src/patterns/closure-privacy.js": "/**\\n * Closures with Practical Data Privacy: Bank Account Module Pattern\\n */\\n\\nfunction createBankAccount(initialBalance, accountHolder) {\\n  // Private variables encapsulated within the outer lexical scope\\n  let balance = Math.max(0, initialBalance || 0);\\n  const owner = accountHolder;\\n  const transactionHistory = [];\\n\\n  function recordTransaction(type, amount) {\\n    transactionHistory.push({\\n      timestamp: new Date().toISOString(),\\n      type,\\n      amount,\\n      balanceAfter: balance,\\n    });\\n  }\\n\\n  recordTransaction(\'OPEN_ACCOUNT\', balance);\\n\\n  // Return public interface (revealing module pattern)\\n  return {\\n    getAccountHolder() {\\n      return owner;\\n    },\\n    getBalance() {\\n      return balance;\\n    },\\n    deposit(amount) {\\n      if (typeof amount !== \'number\' || amount <= 0) {\\n        throw new Error(\'Deposit amount must be a positive number\');\\n      }\\n      balance += amount;\\n      recordTransaction(\'DEPOSIT\', amount);\\n      return balance;\\n    },\\n    withdraw(amount) {\\n      if (typeof amount !== \'number\' || amount <= 0) {\\n        throw new Error(\'Withdrawal amount must be a positive number\');\\n      }\\n      if (amount > balance) {\\n        throw new Error(\'Insufficient funds\');\\n      }\\n      balance -= amount;\\n      recordTransaction(\'WITHDRAW\', amount);\\n      return balance;\\n    },\\n    getTransactions() {\\n      // Return a defensive copy to prevent external mutation\\n      return [...transactionHistory];\\n    }\\n  };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Closures & Data Privacy ---\');\\n\\n  const account = createBankAccount(100, \'Alice\');\\n\\n  // Verify internal variables are completely inaccessible\\n  console.assert(account.balance === undefined, \'balance should be private\');\\n  console.assert(account.owner === undefined, \'owner should be private\');\\n  console.assert(account.transactionHistory === undefined, \'transactionHistory should be private\');\\n\\n  // Test transactions\\n  account.deposit(50);\\n  console.assert(account.getBalance() === 150, \'Deposit failed\');\\n\\n  account.withdraw(30);\\n  console.assert(account.getBalance() === 120, \'Withdraw failed\');\\n\\n  let failedWithdraw = false;\\n  try {\\n    account.withdraw(500);\\n  } catch (err) {\\n    failedWithdraw = true;\\n  }\\n  console.assert(failedWithdraw, \'Overdraft should fail\');\\n\\n  const txs = account.getTransactions();\\n  console.assert(txs.length === 3, \'Transaction count mismatch\');\\n\\n  console.log(\'\\u2713 All Closure data privacy tests passed!\');\\n}\\n\\nmodule.exports = { createBankAccount };\\n",\n    "src/patterns/event-emitter.js": "/**\\n * Pub/Sub (Event Emitter) Pattern in JavaScript\\n */\\n\\nclass EventEmitter {\\n  constructor() {\\n    this.events = new Map();\\n  }\\n\\n  on(eventName, listener) {\\n    if (typeof listener !== \'function\') {\\n      throw new TypeError(\'Listener must be a function\');\\n    }\\n    if (!this.events.has(eventName)) {\\n      this.events.set(eventName, []);\\n    }\\n    this.events.get(eventName).push(listener);\\n\\n    // Unsubscribe handle\\n    return () => this.off(eventName, listener);\\n  }\\n\\n  once(eventName, listener) {\\n    if (typeof listener !== \'function\') {\\n      throw new TypeError(\'Listener must be a function\');\\n    }\\n    const wrapper = (...args) => {\\n      this.off(eventName, wrapper);\\n      listener.apply(this, args);\\n    };\\n    wrapper.originalListener = listener;\\n    return this.on(eventName, wrapper);\\n  }\\n\\n  off(eventName, listener) {\\n    if (!this.events.has(eventName)) return;\\n\\n    const listeners = this.events.get(eventName).filter(\\n      (l) => l !== listener && l.originalListener !== listener\\n    );\\n\\n    if (listeners.length === 0) {\\n      this.events.delete(eventName);\\n    } else {\\n      this.events.set(eventName, listeners);\\n    }\\n  }\\n\\n  emit(eventName, ...args) {\\n    if (!this.events.has(eventName)) return false;\\n\\n    // Clone listeners to avoid mutations during iteration\\n    const listeners = [...this.events.get(eventName)];\\n    for (const listener of listeners) {\\n      listener.apply(this, args);\\n    }\\n    return true;\\n  }\\n\\n  listenerCount(eventName) {\\n    return this.events.has(eventName) ? this.events.get(eventName).length : 0;\\n  }\\n\\n  removeAllListeners(eventName) {\\n    if (eventName) {\\n      this.events.delete(eventName);\\n    } else {\\n      this.events.clear();\\n    }\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing EventEmitter ---\');\\n\\n  const emitter = new EventEmitter();\\n  let callCount = 0;\\n  let receivedData = null;\\n\\n  const unsubscribe = emitter.on(\'data\', (data) => {\\n    callCount++;\\n    receivedData = data;\\n  });\\n\\n  emitter.emit(\'data\', { id: 1 });\\n  console.assert(callCount === 1 && receivedData.id === 1, \'Event emit failed\');\\n\\n  // Once test\\n  let onceCount = 0;\\n  emitter.once(\'single\', () => onceCount++);\\n  emitter.emit(\'single\');\\n  emitter.emit(\'single\');\\n  console.assert(onceCount === 1, \'Once listener failed\');\\n\\n  // Unsubscribe test\\n  unsubscribe();\\n  emitter.emit(\'data\', { id: 2 });\\n  console.assert(callCount === 1, \'Unsubscribe failed\');\\n\\n  console.log(\'\\u2713 All EventEmitter tests passed!\');\\n}\\n\\nmodule.exports = { EventEmitter };\\n",\n    "src/patterns/inheritance.js": "/**\\n * Classical vs Prototypal Inheritance with ES5 and ES6 Classes\\n */\\n\\n// 1. ES5 Pseudo-Classical Pattern\\nfunction VehicleES5(make, model) {\\n  this.make = make;\\n  this.model = model;\\n}\\n\\nVehicleES5.prototype.getDetails = function() {\\n  return this.make + \' \' + this.model;\\n};\\n\\nfunction CarES5(make, model, doors) {\\n  VehicleES5.call(this, make, model);\\n  this.doors = doors;\\n}\\n\\nCarES5.prototype = Object.create(VehicleES5.prototype);\\nCarES5.prototype.constructor = CarES5;\\n\\nCarES5.prototype.getDetails = function() {\\n  return VehicleES5.prototype.getDetails.call(this) + \' with \' + this.doors + \' doors\';\\n};\\n\\n// 2. ES6 Modern Class Pattern\\nclass VehicleES6 {\\n  constructor(make, model) {\\n    this.make = make;\\n    this.model = model;\\n  }\\n\\n  getDetails() {\\n    return `${this.make} ${this.model}`;\\n  }\\n}\\n\\nclass CarES6 extends VehicleES6 {\\n  #vin; // Private field\\n\\n  constructor(make, model, doors, vin) {\\n    super(make, model);\\n    this.doors = doors;\\n    this.#vin = vin;\\n  }\\n\\n  getDetails() {\\n    return `${super.getDetails()} with ${this.doors} doors`;\\n  }\\n\\n  getVin() {\\n    return this.#vin;\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing ES5 vs ES6 Inheritance ---\');\\n\\n  const es5Car = new CarES5(\'Toyota\', \'Corolla\', 4);\\n  console.assert(es5Car.getDetails() === \'Toyota Corolla with 4 doors\', \'ES5 inheritance failed\');\\n  console.assert(es5Car instanceof VehicleES5, \'ES5 instanceof failed\');\\n\\n  const es6Car = new CarES6(\'Tesla\', \'Model 3\', 4, \'VIN12345\');\\n  console.assert(es6Car.getDetails() === \'Tesla Model 3 with 4 doors\', \'ES6 inheritance failed\');\\n  console.assert(es6Car instanceof VehicleES6, \'ES6 instanceof failed\');\\n  console.assert(es6Car.getVin() === \'VIN12345\', \'ES6 private field failed\');\\n\\n  console.log(\'\\u2713 All Inheritance structure tests passed!\');\\n}\\n\\nmodule.exports = { VehicleES5, CarES5, VehicleES6, CarES6 };\\n",\n    "src/patterns/reactive.js": "/**\\n * Simple Reactive Data-Binding Store using Proxy & Reflect\\n */\\n\\nlet activeEffect = null;\\n\\nclass ReactiveStore {\\n  constructor(initialState = {}) {\\n    this.targetMap = new WeakMap();\\n    this.state = this._reactive(initialState);\\n  }\\n\\n  _track(target, key) {\\n    if (!activeEffect) return;\\n\\n    let depsMap = this.targetMap.get(target);\\n    if (!depsMap) {\\n      depsMap = new Map();\\n      this.targetMap.set(target, depsMap);\\n    }\\n\\n    let dep = depsMap.get(key);\\n    if (!dep) {\\n      dep = new Set();\\n      depsMap.set(key, dep);\\n    }\\n\\n    dep.add(activeEffect);\\n  }\\n\\n  _trigger(target, key) {\\n    const depsMap = this.targetMap.get(target);\\n    if (!depsMap) return;\\n\\n    const dep = depsMap.get(key);\\n    if (dep) {\\n      dep.forEach((effect) => effect());\\n    }\\n  }\\n\\n  _reactive(target) {\\n    if (target === null || typeof target !== \'object\') {\\n      return target;\\n    }\\n\\n    const self = this;\\n    return new Proxy(target, {\\n      get(obj, key, receiver) {\\n        const result = Reflect.get(obj, key, receiver);\\n        self._track(obj, key);\\n        return typeof result === \'object\' && result !== null\\n          ? self._reactive(result)\\n          : result;\\n      },\\n      set(obj, key, value, receiver) {\\n        const oldValue = obj[key];\\n        const success = Reflect.set(obj, key, value, receiver);\\n        if (oldValue !== value) {\\n          self._trigger(obj, key);\\n        }\\n        return success;\\n      }\\n    });\\n  }\\n\\n  watch(effectFn) {\\n    activeEffect = effectFn;\\n    effectFn();\\n    activeEffect = null;\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Proxy Data Binding ---\');\\n\\n  const store = new ReactiveStore({ count: 0, text: \'hello\' });\\n  let watchedValue = 0;\\n\\n  store.watch(() => {\\n    watchedValue = store.state.count * 2;\\n  });\\n\\n  console.assert(watchedValue === 0, \'Initial effect execution failed\');\\n\\n  store.state.count = 5;\\n  console.assert(watchedValue === 10, \'Reactive trigger failed\');\\n\\n  store.state.count = 12;\\n  console.assert(watchedValue === 24, \'Secondary reactive trigger failed\');\\n\\n  console.log(\'\\u2713 All Proxy Data Binding tests passed!\');\\n}\\n\\nmodule.exports = { ReactiveStore };\\n",\n    "src/polyfills/call-apply-bind.js": "/**\\n * Custom Polyfills for call, apply, and bind\\n */\\n\\nFunction.prototype.myCall = function(thisArg, ...args) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  // Handle null/undefined thisArg\\n  thisArg = thisArg == null ? globalThis : Object(thisArg);\\n\\n  const fnSymbol = Symbol(\'fn\');\\n  thisArg[fnSymbol] = this;\\n\\n  const result = thisArg[fnSymbol](...args);\\n  delete thisArg[fnSymbol];\\n\\n  return result;\\n};\\n\\nFunction.prototype.myApply = function(thisArg, argsArray) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  thisArg = thisArg == null ? globalThis : Object(thisArg);\\n\\n  const fnSymbol = Symbol(\'fn\');\\n  thisArg[fnSymbol] = this;\\n\\n  let result;\\n  if (!argsArray) {\\n    result = thisArg[fnSymbol]();\\n  } else {\\n    result = thisArg[fnSymbol](...argsArray);\\n  }\\n\\n  delete thisArg[fnSymbol];\\n  return result;\\n};\\n\\nFunction.prototype.myBind = function(thisArg, ...boundArgs) {\\n  if (typeof this !== \'function\') {\\n    throw new TypeError(this + \' is not callable\');\\n  }\\n\\n  const originalFn = this;\\n\\n  function boundFunction(...callArgs) {\\n    // If called as constructor with \'new\', \'this\' should be the new instance\\n    const isNew = this instanceof boundFunction;\\n    const context = isNew ? this : thisArg;\\n    return originalFn.apply(context, [...boundArgs, ...callArgs]);\\n  }\\n\\n  // Maintain prototype chain for \'new\'\\n  if (originalFn.prototype) {\\n    boundFunction.prototype = Object.create(originalFn.prototype);\\n    boundFunction.prototype.constructor = boundFunction;\\n  }\\n\\n  return boundFunction;\\n};\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing call, apply, and bind Polyfills ---\');\\n\\n  function greet(greeting, punctuation) {\\n    return greeting + \', \' + this.name + punctuation;\\n  }\\n\\n  const user = { name: \'Sarah\' };\\n\\n  console.assert(greet.myCall(user, \'Hello\', \'!\') === \'Hello, Sarah!\', \'myCall failed\');\\n  console.assert(greet.myApply(user, [\'Hi\', \'?\']) === \'Hi, Sarah?\', \'myApply failed\');\\n\\n  const bound = greet.myBind(user, \'Hey\');\\n  console.assert(bound(\'.\') === \'Hey, Sarah.\', \'myBind failed\');\\n\\n  // Test constructor with bound function\\n  function Person(age) {\\n    this.age = age;\\n  }\\n  const BoundPerson = Person.myBind({ dummy: true });\\n  const pInstance = new BoundPerson(25);\\n  console.assert(pInstance.age === 25, \'myBind constructor age failed\');\\n  console.assert(pInstance instanceof Person, \'myBind constructor instanceof failed\');\\n\\n  console.log(\'\\u2713 All call, apply, and bind tests passed!\');\\n}\\n\\nmodule.exports = {};\\n",\n    "src/polyfills/filter.js": "/**\\n * Custom Polyfill for Array.prototype.filter\\n * Specification: ECMA-262 22.1.3.7 Array.prototype.filter(callbackfn [ , thisArg ])\\n */\\n\\nfunction myFilter(callback, thisArg) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myFilter called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0;\\n  const result = [];\\n\\n  for (let k = 0; k < len; k++) {\\n    if (k in O) {\\n      const val = O[k];\\n      if (callback.call(thisArg, val, k, O)) {\\n        result.push(val);\\n      }\\n    }\\n  }\\n\\n  return result;\\n}\\n\\nArray.prototype.myFilter = myFilter;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myFilter ---\');\\n\\n  const numbers = [1, 2, 3, 4, 5, 6];\\n  const evens = numbers.myFilter(n => n % 2 === 0);\\n  console.assert(JSON.stringify(evens) === JSON.stringify([2, 4, 6]), \'Filter evens failed\');\\n\\n  // Sparse array handling (skipped empty slots)\\n  const sparse = [10, , 25, , 30]; // eslint-disable-line no-sparse-arrays\\n  const filteredSparse = sparse.myFilter(n => n > 15);\\n  console.assert(JSON.stringify(filteredSparse) === JSON.stringify([25, 30]), \'Sparse filtering failed\');\\n\\n  // Context thisArg validation\\n  const validator = {\\n    threshold: 3,\\n    isAbove(val) { return val > this.threshold; }\\n  };\\n  const filteredWithThis = [1, 2, 4, 5].myFilter(function(n) { return this.isAbove(n); }, validator);\\n  console.assert(JSON.stringify(filteredWithThis) === JSON.stringify([4, 5]), \'thisArg filter failed\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myFilter tests passed!\');\\n}\\n\\nmodule.exports = { myFilter };\\n",\n    "src/polyfills/map.js": "/**\\n * Custom Polyfill for Array.prototype.map\\n * Specification: ECMA-262 22.1.3.16 Array.prototype.map(callbackfn [ , thisArg ])\\n */\\n\\nfunction myMap(callback, thisArg) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myMap called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0; // Unsigned 32-bit integer conversion\\n  const A = new Array(len);\\n\\n  for (let k = 0; k < len; k++) {\\n    // Only invoke callback if index k exists in O (sparse array handling)\\n    if (k in O) {\\n      A[k] = callback.call(thisArg, O[k], k, O);\\n    }\\n  }\\n\\n  return A;\\n}\\n\\nArray.prototype.myMap = myMap;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myMap ---\');\\n  \\n  const numbers = [1, 2, 3, 4];\\n  const doubled = numbers.myMap((num, idx) => num * 2 + idx);\\n  console.assert(JSON.stringify(doubled) === JSON.stringify([2, 5, 8, 11]), \'Basic mapping failed\');\\n\\n  // Sparse array handling\\n  const sparse = [1, , 3]; // eslint-disable-line no-sparse-arrays\\n  const sparseMapped = sparse.myMap(x => x * 2);\\n  console.assert(0 in sparseMapped && !(1 in sparseMapped) && 2 in sparseMapped, \'Sparse mapping failed\');\\n  console.assert(sparseMapped[0] === 2 && sparseMapped[2] === 6, \'Sparse values mismatch\');\\n\\n  // thisArg binding\\n  const multiplier = {\\n    factor: 10,\\n    multiply(n) { return n * this.factor; }\\n  };\\n  const scaled = [2, 3].myMap(function(n) { return this.multiply(n); }, multiplier);\\n  console.assert(JSON.stringify(scaled) === JSON.stringify([20, 30]), \'thisArg binding failed\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myMap tests passed!\');\\n}\\n\\nmodule.exports = { myMap };\\n",\n    "src/polyfills/promise.js": "/**\\n * Custom Polyfill for Promise (Promises/A+ Spec Compliant Core)\\n */\\n\\nconst STATE = {\\n  PENDING: \'pending\',\\n  FULFILLED: \'fulfilled\',\\n  REJECTED: \'rejected\',\\n};\\n\\nclass MyPromise {\\n  constructor(executor) {\\n    if (typeof executor !== \'function\') {\\n      throw new TypeError(\'Promise resolver \' + executor + \' is not a function\');\\n    }\\n\\n    this.state = STATE.PENDING;\\n    this.value = undefined;\\n    this.handlers = [];\\n\\n    const resolve = (val) => {\\n      if (this.state !== STATE.PENDING) return;\\n\\n      // Unwrapping thenables / nested promises\\n      if (val && (typeof val === \'object\' || typeof val === \'function\') && typeof val.then === \'function\') {\\n        val.then(resolve, reject);\\n        return;\\n      }\\n\\n      this.state = STATE.FULFILLED;\\n      this.value = val;\\n      this._executeHandlers();\\n    };\\n\\n    const reject = (reason) => {\\n      if (this.state !== STATE.PENDING) return;\\n\\n      this.state = STATE.REJECTED;\\n      this.value = reason;\\n      this._executeHandlers();\\n    };\\n\\n    try {\\n      executor(resolve, reject);\\n    } catch (err) {\\n      reject(err);\\n    }\\n  }\\n\\n  _executeHandlers() {\\n    if (this.state === STATE.PENDING) return;\\n\\n    // Must be executed asynchronously via microtask\\n    queueMicrotask(() => {\\n      for (const { onFulfilled, onRejected, resolve, reject } of this.handlers) {\\n        try {\\n          if (this.state === STATE.FULFILLED) {\\n            if (typeof onFulfilled === \'function\') {\\n              resolve(onFulfilled(this.value));\\n            } else {\\n              resolve(this.value);\\n            }\\n          } else if (this.state === STATE.REJECTED) {\\n            if (typeof onRejected === \'function\') {\\n              resolve(onRejected(this.value));\\n            } else {\\n              reject(this.value);\\n            }\\n          }\\n        } catch (err) {\\n          reject(err);\\n        }\\n      }\\n      this.handlers = [];\\n    });\\n  }\\n\\n  then(onFulfilled, onRejected) {\\n    return new MyPromise((resolve, reject) => {\\n      this.handlers.push({ onFulfilled, onRejected, resolve, reject });\\n      this._executeHandlers();\\n    });\\n  }\\n\\n  catch(onRejected) {\\n    return this.then(null, onRejected);\\n  }\\n\\n  finally(callback) {\\n    return this.then(\\n      (val) => MyPromise.resolve(callback()).then(() => val),\\n      (err) => MyPromise.resolve(callback()).then(() => { throw err; })\\n    );\\n  }\\n\\n  static resolve(value) {\\n    if (value instanceof MyPromise) return value;\\n    return new MyPromise((res) => res(value));\\n  }\\n\\n  static reject(reason) {\\n    return new MyPromise((_, rej) => rej(reason));\\n  }\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing MyPromise Polyfill ---\');\\n\\n  const p = new MyPromise((resolve) => {\\n    setTimeout(() => resolve(100), 20);\\n  });\\n\\n  p.then((val) => {\\n    console.assert(val === 100, \'Initial resolve value failed\');\\n    return val * 2;\\n  })\\n  .then((val2) => {\\n    console.assert(val2 === 200, \'Chaining value failed\');\\n    console.log(\'\\u2713 All MyPromise tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { MyPromise };\\n",\n    "src/polyfills/reduce.js": "/**\\n * Custom Polyfill for Array.prototype.reduce\\n * Specification: ECMA-262 22.1.3.18 Array.prototype.reduce(callbackfn [ , initialValue ])\\n */\\n\\nfunction myReduce(callback, initialValue) {\\n  if (this == null) {\\n    throw new TypeError(\'Array.prototype.myReduce called on null or undefined\');\\n  }\\n  if (typeof callback !== \'function\') {\\n    throw new TypeError(callback + \' is not a function\');\\n  }\\n\\n  const O = Object(this);\\n  const len = O.length >>> 0;\\n  let k = 0;\\n  let accumulator;\\n\\n  // Determine accumulator and start index\\n  if (arguments.length >= 2) {\\n    accumulator = initialValue;\\n  } else {\\n    let kPresent = false;\\n    while (k < len && !kPresent) {\\n      if (k in O) {\\n        accumulator = O[k];\\n        kPresent = true;\\n      }\\n      k++;\\n    }\\n    if (!kPresent) {\\n      throw new TypeError(\'Reduce of empty array with no initial value\');\\n    }\\n  }\\n\\n  while (k < len) {\\n    if (k in O) {\\n      accumulator = callback(accumulator, O[k], k, O);\\n    }\\n    k++;\\n  }\\n\\n  return accumulator;\\n}\\n\\nArray.prototype.myReduce = myReduce;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Array.prototype.myReduce ---\');\\n\\n  const sum = [1, 2, 3, 4].myReduce((acc, curr) => acc + curr, 0);\\n  console.assert(sum === 10, \'Basic sum reduce failed\');\\n\\n  const product = [2, 3, 4].myReduce((acc, curr) => acc * curr);\\n  console.assert(product === 24, \'Reduce without initialValue failed\');\\n\\n  // Object tally accumulation\\n  const fruits = [\'apple\', \'banana\', \'apple\', \'orange\', \'banana\', \'apple\'];\\n  const tally = fruits.myReduce((acc, fruit) => {\\n    acc[fruit] = (acc[fruit] || 0) + 1;\\n    return acc;\\n  }, {});\\n  console.assert(tally.apple === 3 && tally.banana === 2 && tally.orange === 1, \'Tally reduce failed\');\\n\\n  // Empty array without initial value check\\n  let threw = false;\\n  try {\\n    [].myReduce(x => x);\\n  } catch (e) {\\n    threw = e instanceof TypeError;\\n  }\\n  console.assert(threw, \'Expected TypeError on empty array with no initial value\');\\n\\n  console.log(\'\\u2713 All Array.prototype.myReduce tests passed!\');\\n}\\n\\nmodule.exports = { myReduce };\\n",\n    "src/puzzles/algorithms.js": "/**\\n * Common JavaScript Algorithmic Puzzles:\\n * 1. Longest Substring Without Repeating Characters (Sliding Window)\\n * 2. Two Sum (Hash Map Lookup)\\n * 3. Maximum Subarray Sum (Kadane\'s Algorithm)\\n */\\n\\n// 1. Longest Substring Without Repeating Characters - O(n)\\nfunction lengthOfLongestSubstring(s) {\\n  const charMap = new Map();\\n  let maxLength = 0;\\n  let windowStart = 0;\\n\\n  for (let windowEnd = 0; windowEnd < s.length; windowEnd++) {\\n    const rightChar = s[windowEnd];\\n    if (charMap.has(rightChar)) {\\n      windowStart = Math.max(windowStart, charMap.get(rightChar) + 1);\\n    }\\n    charMap.set(rightChar, windowEnd);\\n    maxLength = Math.max(maxLength, windowEnd - windowStart + 1);\\n  }\\n\\n  return maxLength;\\n}\\n\\n// 2. Two Sum - O(n)\\nfunction twoSum(nums, target) {\\n  const seen = new Map();\\n  for (let i = 0; i < nums.length; i++) {\\n    const complement = target - nums[i];\\n    if (seen.has(complement)) {\\n      return [seen.get(complement), i];\\n    }\\n    seen.set(nums[i], i);\\n  }\\n  return [];\\n}\\n\\n// 3. Maximum Subarray Sum (Kadane\'s) - O(n)\\nfunction maxSubArray(nums) {\\n  if (nums.length === 0) return 0;\\n  let currentMax = nums[0];\\n  let globalMax = nums[0];\\n\\n  for (let i = 1; i < nums.length; i++) {\\n    currentMax = Math.max(nums[i], currentMax + nums[i]);\\n    globalMax = Math.max(globalMax, currentMax);\\n  }\\n\\n  return globalMax;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Algorithmic Puzzles ---\');\\n\\n  console.assert(lengthOfLongestSubstring(\'abcabcbb\') === 3, \'Longest substring 1 failed\');\\n  console.assert(lengthOfLongestSubstring(\'bbbbb\') === 1, \'Longest substring 2 failed\');\\n  console.assert(lengthOfLongestSubstring(\'pwwkew\') === 3, \'Longest substring 3 failed\');\\n\\n  const sumIndices = twoSum([2, 7, 11, 15], 9);\\n  console.assert(sumIndices[0] === 0 && sumIndices[1] === 1, \'Two Sum failed\');\\n\\n  const maxSub = maxSubArray([-2, 1, -3, 4, -1, 2, 1, -5, 4]);\\n  console.assert(maxSub === 6, \'Max subarray failed\'); // [4, -1, 2, 1]\\n\\n  console.log(\'\\u2713 All Algorithmic Puzzle tests passed!\');\\n}\\n\\nmodule.exports = { lengthOfLongestSubstring, twoSum, maxSubArray };\\n",\n    "src/utils/compose-pipe.js": "/**\\n * Function Composition (Right-to-Left) and Piping (Left-to-Right)\\n */\\n\\nfunction compose(...fns) {\\n  if (fns.length === 0) return (arg) => arg;\\n  if (fns.length === 1) return fns[0];\\n\\n  return function(initialValue) {\\n    return fns.reduceRight((acc, fn) => fn(acc), initialValue);\\n  };\\n}\\n\\nfunction pipe(...fns) {\\n  if (fns.length === 0) return (arg) => arg;\\n  if (fns.length === 1) return fns[0];\\n\\n  return function(initialValue) {\\n    return fns.reduce((acc, fn) => fn(acc), initialValue);\\n  };\\n}\\n\\n// Asynchronous pipe for Promise chains\\nfunction pipeAsync(...fns) {\\n  return function(initialValue) {\\n    return fns.reduce(\\n      (promiseChain, fn) => promiseChain.then(fn),\\n      Promise.resolve(initialValue)\\n    );\\n  };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Compose and Pipe ---\');\\n\\n  const add5 = (x) => x + 5;\\n  const multiply2 = (x) => x * 2;\\n  const square = (x) => x * x;\\n\\n  // compose executes right-to-left: square(multiply2(add5(2))) -> (2+5)=7 -> 7*2=14 -> 14^2=196\\n  const composed = compose(square, multiply2, add5);\\n  console.assert(composed(2) === 196, \'Compose failed\');\\n\\n  // pipe executes left-to-right: square(2)=4 -> add5(4)=9 -> multiply2(9)=18\\n  const piped = pipe(square, add5, multiply2);\\n  console.assert(piped(2) === 18, \'Pipe failed\');\\n\\n  // Async pipe\\n  const asyncAdd = async (x) => x + 10;\\n  const asyncDouble = async (x) => x * 2;\\n  pipeAsync(asyncAdd, asyncDouble)(5).then((val) => {\\n    console.assert(val === 30, \'Async pipe failed\');\\n    console.log(\'\\u2713 All Compose & Pipe tests passed!\');\\n  });\\n}\\n\\nmodule.exports = { compose, pipe, pipeAsync };\\n",\n    "src/utils/curry.js": "/**\\n * Currying Utility with Placeholder Support\\n */\\n\\nconst _ = Symbol(\'curry_placeholder\');\\n\\nfunction curry(fn, arity = fn.length) {\\n  return function curried(...args) {\\n    // Count real (non-placeholder) arguments\\n    const complete = args.length >= arity &&\\n      args.slice(0, arity).every((arg) => arg !== _);\\n\\n    if (complete) {\\n      return fn.apply(this, args.slice(0, arity));\\n    }\\n\\n    return function(...nextArgs) {\\n      // Merge previous args and nextArgs, filling placeholders\\n      const combinedArgs = [];\\n      let nextIndex = 0;\\n\\n      for (let i = 0; i < args.length; i++) {\\n        if (args[i] === _ && nextIndex < nextArgs.length) {\\n          combinedArgs.push(nextArgs[nextIndex++]);\\n        } else {\\n          combinedArgs.push(args[i]);\\n        }\\n      }\\n\\n      while (nextIndex < nextArgs.length) {\\n        combinedArgs.push(nextArgs[nextIndex++]);\\n      }\\n\\n      return curried.apply(this, combinedArgs);\\n    };\\n  };\\n}\\n\\ncurry._ = _;\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Curry with Placeholder ---\');\\n\\n  function calculate(a, b, c) {\\n    return a + (b * c);\\n  }\\n\\n  const curriedCalc = curry(calculate);\\n\\n  console.assert(curriedCalc(2)(3)(4) === 14, \'Standard curry failed\');\\n  console.assert(curriedCalc(2, 3, 4) === 14, \'Direct call failed\');\\n\\n  // Placeholder tests\\n  const withPlaceholder = curriedCalc(curry._, 3)(2)(4);\\n  console.assert(withPlaceholder === 14, \'Single placeholder failed\');\\n\\n  const multiPlaceholder = curriedCalc(curry._, curry._, 5)(10, 2);\\n  console.assert(multiPlaceholder === 20, \'Multi placeholder failed\');\\n\\n  console.log(\'\\u2713 All Curry tests passed!\');\\n}\\n\\nmodule.exports = { curry, _ };\\n",\n    "src/utils/deep-clone.js": "/**\\n * Custom Deep Clone (deepCopy) Utility\\n * Handles circular references, Map, Set, Date, RegExp, and primitive types.\\n */\\n\\nfunction deepClone(value, hash = new WeakMap()) {\\n  // Primitives and functions\\n  if (value === null || typeof value !== \'object\') {\\n    return value;\\n  }\\n\\n  // Circular reference detection\\n  if (hash.has(value)) {\\n    return hash.get(value);\\n  }\\n\\n  // Dates\\n  if (value instanceof Date) {\\n    return new Date(value.getTime());\\n  }\\n\\n  // Regular Expressions\\n  if (value instanceof RegExp) {\\n    return new RegExp(value.source, value.flags);\\n  }\\n\\n  // Maps\\n  if (value instanceof Map) {\\n    const copyMap = new Map();\\n    hash.set(value, copyMap);\\n    value.forEach((val, key) => {\\n      copyMap.set(deepClone(key, hash), deepClone(val, hash));\\n    });\\n    return copyMap;\\n  }\\n\\n  // Sets\\n  if (value instanceof Set) {\\n    const copySet = new Set();\\n    hash.set(value, copySet);\\n    value.forEach((val) => {\\n      copySet.add(deepClone(val, hash));\\n    });\\n    return copySet;\\n  }\\n\\n  // Arrays and Plain Objects\\n  const isArray = Array.isArray(value);\\n  const copy = isArray ? [] : Object.create(Object.getPrototypeOf(value));\\n  hash.set(value, copy);\\n\\n  // Copy Symbols and regular keys\\n  const keys = [...Object.keys(value), ...Object.getOwnPropertySymbols(value)];\\n  for (const key of keys) {\\n    copy[key] = deepClone(value[key], hash);\\n  }\\n\\n  return copy;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing deepClone Utility ---\');\\n\\n  const original = {\\n    num: 42,\\n    str: \'hello\',\\n    date: new Date(\'2026-01-16\'),\\n    regex: /abc/gi,\\n    nested: { arr: [1, 2, { a: \'b\' }] },\\n    set: new Set([1, 2, 3]),\\n    map: new Map([[\'key\', { inner: \'val\' }]]),\\n  };\\n\\n  // Circular reference\\n  original.self = original;\\n\\n  const cloned = deepClone(original);\\n\\n  console.assert(cloned !== original, \'Cloned must have distinct reference\');\\n  console.assert(cloned.self === cloned, \'Circular reference preserved\');\\n  console.assert(cloned.nested.arr !== original.nested.arr, \'Nested array cloned\');\\n  console.assert(cloned.date.getTime() === original.date.getTime(), \'Date copied\');\\n  console.assert(cloned.map.get(\'key\').inner === \'val\', \'Map copied\');\\n  console.assert(cloned.map.get(\'key\') !== original.map.get(\'key\'), \'Map inner value deep cloned\');\\n\\n  console.log(\'\\u2713 All deepClone tests passed!\');\\n}\\n\\nmodule.exports = { deepClone };\\n",\n    "src/utils/flatten.js": "/**\\n * Object Flattening and Unflattening Utility for Deeply Nested Objects\\n */\\n\\nfunction flattenObject(obj, prefix = \'\', result = {}) {\\n  if (obj === null || typeof obj !== \'object\') {\\n    if (prefix) result[prefix] = obj;\\n    return result;\\n  }\\n\\n  for (const key of Object.keys(obj)) {\\n    const val = obj[key];\\n    const newKey = prefix ? prefix + \'.\' + key : key;\\n\\n    if (val !== null && typeof val === \'object\' && !(val instanceof Date) && !(val instanceof RegExp)) {\\n      flattenObject(val, newKey, result);\\n    } else {\\n      result[newKey] = val;\\n    }\\n  }\\n\\n  return result;\\n}\\n\\nfunction unflattenObject(flatObj) {\\n  const result = {};\\n\\n  for (const [keyPath, value] of Object.entries(flatObj)) {\\n    const keys = keyPath.split(\'.\');\\n    let current = result;\\n\\n    for (let i = 0; i < keys.length; i++) {\\n      const k = keys[i];\\n      if (i === keys.length - 1) {\\n        current[k] = value;\\n      } else {\\n        const nextKey = keys[i + 1];\\n        const isNextArray = !isNaN(Number(nextKey));\\n        if (!current[k]) {\\n          current[k] = isNextArray ? [] : {};\\n        }\\n        current = current[k];\\n      }\\n    }\\n  }\\n\\n  return result;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Object Flattening ---\');\\n\\n  const nested = {\\n    user: {\\n      profile: {\\n        name: \'Alex\',\\n        address: {\\n          city: \'San Francisco\',\\n          zip: \'94105\'\\n        }\\n      },\\n      tags: [\'engineer\', \'javascript\']\\n    }\\n  };\\n\\n  const flattened = flattenObject(nested);\\n  console.assert(flattened[\'user.profile.name\'] === \'Alex\', \'Name flatten failed\');\\n  console.assert(flattened[\'user.profile.address.city\'] === \'San Francisco\', \'City flatten failed\');\\n  console.assert(flattened[\'user.tags.0\'] === \'engineer\', \'Array index flatten failed\');\\n\\n  const unflattened = unflattenObject(flattened);\\n  console.assert(unflattened.user.profile.name === \'Alex\', \'Unflatten name failed\');\\n  console.assert(unflattened.user.tags[0] === \'engineer\', \'Unflatten array failed\');\\n\\n  console.log(\'\\u2713 All Object Flattening tests passed!\');\\n}\\n\\nmodule.exports = { flattenObject, unflattenObject };\\n",\n    "src/utils/hoisting.js": "/**\\n * Hoisting Demonstration: var vs let/const & Functions\\n */\\n\\nfunction demonstrateVarHoisting() {\\n  // var is hoisted and initialized to undefined\\n  console.log(\'var before declaration:\', hoistedVar); // undefined\\n  var hoistedVar = \'I am var\';\\n  console.log(\'var after declaration:\', hoistedVar);   // \'I am var\'\\n  return hoistedVar;\\n}\\n\\nfunction demonstrateTDZ() {\\n  // let and const are hoisted but uninitialized (in TDZ)\\n  let result = \'TDZ Active\';\\n  try {\\n    // Attempting to access tdzLet here throws ReferenceError\\n    // console.log(tdzLet);\\n    eval(\'console.log(tdzLet)\');\\n  } catch (err) {\\n    result = err.name; // ReferenceError\\n  }\\n  let tdzLet = \'Safe now\';\\n  return { result, tdzLet };\\n}\\n\\nfunction demonstrateFunctionHoisting() {\\n  // Function declarations are hoisted completely with body\\n  const fnResult = declaredFunction(); // works!\\n\\n  function declaredFunction() {\\n    return \'Function declaration hoisted!\';\\n  }\\n\\n  // Function expressions with var are hoisted as undefined\\n  let expressionError = null;\\n  try {\\n    expressionFunction(); // TypeError: expressionFunction is not a function\\n  } catch (err) {\\n    expressionError = err.name;\\n  }\\n\\n  var expressionFunction = function() {\\n    return \'Function expression\';\\n  };\\n\\n  return { fnResult, expressionError, expressionFunction: expressionFunction() };\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Hoisting Scenarios ---\');\\n\\n  const varRes = demonstrateVarHoisting();\\n  console.assert(varRes === \'I am var\', \'Var hoisting test failed\');\\n\\n  const tdzRes = demonstrateTDZ();\\n  console.assert(tdzRes.result === \'ReferenceError\', \'TDZ ReferenceError test failed\');\\n  console.assert(tdzRes.tdzLet === \'Safe now\', \'TDZ post declaration failed\');\\n\\n  const fnRes = demonstrateFunctionHoisting();\\n  console.assert(fnRes.fnResult === \'Function declaration hoisted!\', \'Function declaration hoisting failed\');\\n  console.assert(fnRes.expressionError === \'TypeError\', \'Function expression error failed\');\\n\\n  console.log(\'\\u2713 All Hoisting demonstrations passed!\');\\n}\\n\\nmodule.exports = {\\n  demonstrateVarHoisting,\\n  demonstrateTDZ,\\n  demonstrateFunctionHoisting,\\n};\\n",\n    "src/utils/lazy.js": "/**\\n * Custom Lazy Loading Utilities for Heavy Computations and Resources\\n */\\n\\n// 1. Lazy evaluation wrapper (thunk with memoized return)\\nfunction lazy(fn) {\\n  let evaluated = false;\\n  let cachedResult;\\n\\n  return function() {\\n    if (!evaluated) {\\n      cachedResult = fn.apply(this, arguments);\\n      evaluated = true;\\n    }\\n    return cachedResult;\\n  };\\n}\\n\\n// 2. Define lazy property on an object that evaluates once upon first property access\\nfunction defineLazyProperty(target, propertyName, getterFn) {\\n  Object.defineProperty(target, propertyName, {\\n    configurable: true,\\n    enumerable: true,\\n    get() {\\n      const value = getterFn.call(this);\\n      // Replace accessor with concrete value to avoid future getter overhead\\n      Object.defineProperty(this, propertyName, {\\n        value,\\n        writable: true,\\n        configurable: true,\\n        enumerable: true,\\n      });\\n      return value;\\n    }\\n  });\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Lazy Loading Utilities ---\');\\n\\n  let computationCount = 0;\\n  const heavyCalculation = lazy(() => {\\n    computationCount++;\\n    return 42 * 100;\\n  });\\n\\n  console.assert(computationCount === 0, \'Should not evaluate before call\');\\n  console.assert(heavyCalculation() === 4200, \'Calculation mismatch\');\\n  console.assert(computationCount === 1, \'Evaluated once\');\\n  console.assert(heavyCalculation() === 4200, \'Second call mismatch\');\\n  console.assert(computationCount === 1, \'Should not re-evaluate on subsequent calls\');\\n\\n  // Test lazy property\\n  const service = {};\\n  let propComputeCount = 0;\\n  defineLazyProperty(service, \'databaseConnection\', () => {\\n    propComputeCount++;\\n    return { connected: true, id: \'db-101\' };\\n  });\\n\\n  console.assert(propComputeCount === 0, \'Property should not compute eagerly\');\\n  console.assert(service.databaseConnection.connected === true, \'Property access failed\');\\n  console.assert(propComputeCount === 1, \'Property should compute once\');\\n  console.assert(service.databaseConnection.id === \'db-101\', \'Cached property access failed\');\\n  console.assert(propComputeCount === 1, \'Property should remain cached\');\\n\\n  console.log(\'\\u2713 All Lazy Loader tests passed!\');\\n}\\n\\nmodule.exports = { lazy, defineLazyProperty };\\n",\n    "src/utils/memoize.js": "/**\\n * Optimized Closure-based Memoization Utility\\n * Supports custom key serialization, cache size limit (LRU eviction), and hit statistics.\\n */\\n\\nfunction memoize(fn, options = {}) {\\n  const {\\n    maxSize = 100,\\n    resolver = (...args) => {\\n      if (args.length === 0) return \'__empty__\';\\n      if (args.length === 1 && (typeof args[0] === \'number\' || typeof args[0] === \'string\')) {\\n        return args[0];\\n      }\\n      return JSON.stringify(args);\\n    }\\n  } = options;\\n\\n  // Map maintains insertion order for LRU cache eviction\\n  const cache = new Map();\\n  let hits = 0;\\n  let misses = 0;\\n\\n  const memoized = function(...args) {\\n    const key = resolver(...args);\\n\\n    if (cache.has(key)) {\\n      hits++;\\n      const val = cache.get(key);\\n      // Refresh order for LRU\\n      cache.delete(key);\\n      cache.set(key, val);\\n      return val;\\n    }\\n\\n    misses++;\\n    const result = fn.apply(this, args);\\n\\n    if (cache.size >= maxSize) {\\n      // Evict oldest item (first entry in Map)\\n      const oldestKey = cache.keys().next().value;\\n      cache.delete(oldestKey);\\n    }\\n\\n    cache.set(key, result);\\n    return result;\\n  };\\n\\n  memoized.getStats = () => ({ hits, misses, size: cache.size });\\n  memoized.clear = () => {\\n    cache.clear();\\n    hits = 0;\\n    misses = 0;\\n  };\\n\\n  return memoized;\\n}\\n\\n// Test cases & Demonstrations\\nif (require.main === module) {\\n  console.log(\'--- Testing Optimized Memoize ---\');\\n\\n  let callCount = 0;\\n  const fib = memoize((n) => {\\n    callCount++;\\n    if (n <= 1) return n;\\n    return fib(n - 1) + fib(n - 2);\\n  });\\n\\n  const res1 = fib(10);\\n  console.assert(res1 === 55, \'Fib calculation failed\');\\n  const stats1 = fib.getStats();\\n  console.assert(stats1.hits > 0, \'Cache hits expected\');\\n\\n  // Test custom serializer and LRU eviction\\n  let squareCalls = 0;\\n  const square = memoize((x) => {\\n    squareCalls++;\\n    return x * x;\\n  }, { maxSize: 2 });\\n\\n  square(2); // miss, size 1 [2]\\n  square(3); // miss, size 2 [2, 3]\\n  square(2); // hit, size 2  [3, 2]\\n  square(4); // miss, size 2 (evicts 3) [2, 4]\\n\\n  console.assert(squareCalls === 3, \'LRU square calls mismatch\');\\n  console.assert(square.getStats().hits === 1, \'LRU hits mismatch\');\\n\\n  console.log(\'\\u2713 All Memoize tests passed!\');\\n}\\n\\nmodule.exports = { memoize };\\n",\n    "scripts/test_runner.js": "/**\\n * Automated Test Runner for all 31-Day Modules\\n */\\n\\nconst { myMap } = require(\'../src/polyfills/map\');\\nconst { myFilter } = require(\'../src/polyfills/filter\');\\nconst { myReduce } = require(\'../src/polyfills/reduce\');\\nconst { MyPromise } = require(\'../src/polyfills/promise\');\\nconst { debounce } = require(\'../src/async/debounce\');\\nconst { throttle } = require(\'../src/async/throttle\');\\nconst { promiseAll, promiseRace, promiseAllSettled } = require(\'../src/async/promise-combinators\');\\nconst { createBankAccount } = require(\'../src/patterns/closure-privacy\');\\nconst { EventEmitter } = require(\'../src/patterns/event-emitter\');\\nconst { ReactiveStore } = require(\'../src/patterns/reactive\');\\nconst { memoize } = require(\'../src/utils/memoize\');\\nconst { deepClone } = require(\'../src/utils/deep-clone\');\\nconst { flattenObject, unflattenObject } = require(\'../src/utils/flatten\');\\nconst { curry } = require(\'../src/utils/curry\');\\nconst { compose, pipe } = require(\'../src/utils/compose-pipe\');\\nconst { lazy } = require(\'../src/utils/lazy\');\\nconst { lengthOfLongestSubstring, twoSum, maxSubArray } = require(\'../src/puzzles/algorithms\');\\n\\nconsole.log(\'==================================================\');\\nconsole.log(\'   RUNNING ALL 31-DAY JAVASCRIPT MASTER SUITE    \');\\nconsole.log(\'==================================================\\\\n\');\\n\\nlet passedCount = 0;\\nfunction test(name, fn) {\\n  try {\\n    fn();\\n    console.log(\'  \\u2713 \' + name);\\n    passedCount++;\\n  } catch (err) {\\n    console.error(\'  \\u2717 \' + name + \' FAILED:\', err.message);\\n    process.exit(1);\\n  }\\n}\\n\\ntest(\'Day 1: Array.prototype.myMap\', () => {\\n  const res = [1, 2, 3].myMap(x => x * 3);\\n  if (res[0] !== 3 || res[1] !== 6 || res[2] !== 9) throw new Error(\'myMap failed\');\\n});\\n\\ntest(\'Day 2: Array.prototype.myFilter\', () => {\\n  const res = [1, 2, 3, 4].myFilter(x => x % 2 === 0);\\n  if (res.length !== 2 || res[0] !== 2 || res[1] !== 4) throw new Error(\'myFilter failed\');\\n});\\n\\ntest(\'Day 3: Array.prototype.myReduce\', () => {\\n  const sum = [1, 2, 3, 4].myReduce((a, b) => a + b, 10);\\n  if (sum !== 20) throw new Error(\'myReduce failed\');\\n});\\n\\ntest(\'Day 6: Closure Bank Account Privacy\', () => {\\n  const acct = createBankAccount(50, \'Bob\');\\n  acct.deposit(25);\\n  if (acct.getBalance() !== 75) throw new Error(\'Deposit failed\');\\n  if (acct.balance !== undefined) throw new Error(\'balance leaked\');\\n});\\n\\ntest(\'Day 7: Optimized Memoize with LRU\', () => {\\n  let runs = 0;\\n  const square = memoize(x => { runs++; return x * x; });\\n  square(4);\\n  square(4);\\n  if (runs !== 1) throw new Error(\'Memoize cache missed\');\\n});\\n\\ntest(\'Day 16: Deep Clone with Circular Reference\', () => {\\n  const o = { a: 1 };\\n  o.self = o;\\n  const c = deepClone(o);\\n  if (c.a !== 1 || c.self !== c || c === o) throw new Error(\'Deep clone failed\');\\n});\\n\\ntest(\'Day 17: Flatten & Unflatten Object\', () => {\\n  const flat = flattenObject({ a: { b: 42 } });\\n  if (flat[\'a.b\'] !== 42) throw new Error(\'Flatten failed\');\\n  const unflat = unflattenObject(flat);\\n  if (unflat.a.b !== 42) throw new Error(\'Unflatten failed\');\\n});\\n\\ntest(\'Day 22: Event Emitter Pub/Sub\', () => {\\n  const ee = new EventEmitter();\\n  let count = 0;\\n  const off = ee.on(\'test\', () => count++);\\n  ee.emit(\'test\');\\n  off();\\n  ee.emit(\'test\');\\n  if (count !== 1) throw new Error(\'EventEmitter failed\');\\n});\\n\\ntest(\'Day 23: Curry with Placeholder\', () => {\\n  const f = curry((a, b, c) => a + b + c);\\n  if (f(1)(2)(3) !== 6) throw new Error(\'Curry failed\');\\n  if (f(curry._, 2)(1, 3) !== 6) throw new Error(\'Placeholder curry failed\');\\n});\\n\\ntest(\'Day 24: Compose & Pipe\', () => {\\n  const add1 = x => x + 1;\\n  const mult2 = x => x * 2;\\n  if (compose(mult2, add1)(3) !== 8) throw new Error(\'Compose failed\');\\n  if (pipe(add1, mult2)(3) !== 8) throw new Error(\'Pipe failed\');\\n});\\n\\ntest(\'Day 26: Reactive Proxy Store\', () => {\\n  const store = new ReactiveStore({ val: 1 });\\n  let watched = 0;\\n  store.watch(() => { watched = store.state.val * 10; });\\n  store.state.val = 3;\\n  if (watched !== 30) throw new Error(\'Reactive proxy store failed\');\\n});\\n\\ntest(\'Day 27: Lazy Loader\', () => {\\n  let evaluated = 0;\\n  const lz = lazy(() => { evaluated++; return 99; });\\n  if (evaluated !== 0) throw new Error(\'Lazy evaluated too early\');\\n  lz();\\n  lz();\\n  if (evaluated !== 1) throw new Error(\'Lazy cache failed\');\\n});\\n\\ntest(\'Day 29: Algorithms & Puzzles\', () => {\\n  if (lengthOfLongestSubstring(\'abcabcbb\') !== 3) throw new Error(\'Sliding window failed\');\\n  const indices = twoSum([2, 7, 11, 15], 9);\\n  if (indices[0] !== 0 || indices[1] !== 1) throw new Error(\'Two sum failed\');\\n  if (maxSubArray([-2, 1, -3, 4, -1, 2, 1, -5, 4]) !== 6) throw new Error(\'Max subarray failed\');\\n});\\n\\nconsole.log(\'\\\\n\' + passedCount + \' tests passed successfully! 100% verification complete.\');\\n"\n  },\n  "day31_files": {\n    "README.md": "# 31-Day JavaScript Mastery Series & Interview Handbook\\n\\nA comprehensive, production-grade 31-day JavaScript deep-dive covering language internals, custom polyfills, asynchronous architecture, design patterns, and algorithmic problem-solving.\\n\\n---\\n\\n## January 2026 Commit Calendar & Curriculum\\n\\n| Day | Date | Type | Commit Message & Description |\\n|:---|:---|:---|:---|\\n| **Day 1** | Jan 1, 2026 | `feat` | `feat: implement custom polyfill for Array.prototype.map` \\u2014 Robust sparse array, index callback, and `thisArg` handling. |\\n| **Day 2** | Jan 2, 2026 | `feat` | `feat: implement custom polyfill for Array.prototype.filter` \\u2014 Filtering with predicate validation and sparse slot preservation. |\\n| **Day 3** | Jan 3, 2026 | `feat` | `feat: implement custom polyfill for Array.prototype.reduce` \\u2014 Empty array handling, accumulator initialization, sparse index support. |\\n| **Day 4** | Jan 4, 2026 | `docs` | `docs: add explanations and visual flow for JavaScript Execution Context and Call Stack` \\u2014 Memory creation, execution phase, and call stack diagrams. |\\n| **Day 5** | Jan 5, 2026 | `feat` | `feat: write implementation examples demonstrating Hoisting (var vs let/const)` \\u2014 Temporal Dead Zone (TDZ) and function declaration rules. |\\n| **Day 6** | Jan 6, 2026 | `feat` | `feat: implement Closures with practical data privacy use cases (module pattern)` \\u2014 Bank ledger module with encapsulated state. |\\n| **Day 7** | Jan 7, 2026 | `refactor` | `refactor: optimize closure-based memoization utility function` \\u2014 LRU cache eviction, variadic serializers, and performance stats. |\\n| **Day 8** | Jan 8, 2026 | `docs` | `docs: contrast Scope Chain and Lexical Scoping with code snippets` \\u2014 Static scoping rules, outer lexical links, and variable shadowing. |\\n| **Day 9** | Jan 9, 2026 | `feat` | `feat: implement custom debounce function with leading and trailing options` \\u2014 Includes `.cancel()`, `.flush()`, and `.pending()` APIs. |\\n| **Day 10** | Jan 10, 2026 | `feat` | `feat: implement custom throttle function using timestamp and setTimeout approaches` \\u2014 Dual leading/trailing execution guarantees. |\\n| **Day 11** | Jan 11, 2026 | `docs` | `docs: document how the Event Loop, Microtask Queue, and Callback Queue work` \\u2014 Event loop phases, macrotask vs microtask starvation. |\\n| **Day 12** | Jan 12, 2026 | `feat` | `feat: code examples tracing asynchronous execution order (Promise vs setTimeout)` \\u2014 Step-by-step tracing puzzles and verification. |\\n| **Day 13** | Jan 13, 2026 | `feat` | `feat: implement custom polyfill for Promise (resolving, rejecting, chaining)` \\u2014 Promises/A+ core with asynchronous microtask resolution. |\\n| **Day 14** | Jan 14, 2026 | `feat` | `feat: implement Promise.all, Promise.race, and Promise.allSettled from scratch` \\u2014 Combinators with short-circuiting and settlement tracking. |\\n| **Day 15** | Jan 15, 2026 | `docs` | `docs: add deep-dive notes on async/await vs Generators` \\u2014 Generator coroutines, bi-directional yielding, and custom async runners. |\\n| **Day 16** | Jan 16, 2026 | `feat` | `feat: implement custom deep clone (deepCopy) utility handling circular references` \\u2014 WeakMap cycle tracking for Maps, Sets, Dates, and Regexes. |\\n| **Day 17** | Jan 17, 2026 | `feat` | `feat: implement object flattening function for deeply nested JSON objects` \\u2014 Dot and bracket notation flattening with inverse unflattening. |\\n| **Day 18** | Jan 18, 2026 | `docs` | `docs: explain Prototypal Inheritance and the Prototype Chain with code examples` \\u2014 `__proto__`, `prototype`, and `Object.create()` delegation. |\\n| **Day 19** | Jan 19, 2026 | `feat` | `feat: implement custom polyfill for the JavaScript \'call\', \'apply\', and \'bind\' methods` \\u2014 Symbol property binding and `new` constructor compatibility. |\\n| **Day 20** | Jan 20, 2026 | `feat` | `feat: implement classical vs prototypal inheritance structures using ES6 classes` \\u2014 ES5 prototype chaining side-by-side with ES6 class `extends`. |\\n| **Day 21** | Jan 21, 2026 | `docs` | `docs: document \'this\' keyword behavior across global, object, arrow functions, and event listeners` \\u2014 Default, implicit, explicit, and lexical `this`. |\\n| **Day 22** | Jan 22, 2026 | `feat` | `feat: implement a pub/sub (Event Emitter) design pattern in JavaScript` \\u2014 `on`, `once`, `off`, `emit`, and unsubscribe handles. |\\n| **Day 23** | Jan 23, 2026 | `feat` | `feat: build a currying utility function with placeholder support` \\u2014 Dynamic arity, currying, and `curry._` placeholder resolution. |\\n| **Day 24** | Jan 24, 2026 | `feat` | `feat: implement function composition and piping helper utilities` \\u2014 Right-to-left `compose`, left-to-right `pipe`, and async pipeline. |\\n| **Day 25** | Jan 25, 2026 | `docs` | `docs: break down Garbage Collection mechanisms (Mark-and-Sweep, memory leaks)` \\u2014 V8 Young vs Old space, Cheney scavenge, and leak patterns. |\\n| **Day 26** | Jan 26, 2026 | `feat` | `feat: implement a simple Proxy and Reflect API data-binding mechanism` \\u2014 Reactive store, dependency tracking, and automatic effect dispatch. |\\n| **Day 27** | Jan 27, 2026 | `feat` | `feat: implement custom lazy loading utility for heavy operations` \\u2014 Thunk evaluation and lazy accessor self-overwriting properties. |\\n| **Day 28** | Jan 28, 2026 | `docs` | `docs: outline structural differences between WeakMap, WeakSet, Map, and Set` \\u2014 Garbage collection implications and non-enumerable mechanics. |\\n| **Day 29** | Jan 29, 2026 | `feat` | `feat: solve common JS array/string algorithmic puzzle questions (e.g., sliding window)` \\u2014 Longest Substring, Two Sum, Kadane\'s Max Subarray. |\\n| **Day 30** | Jan 30, 2026 | `refactor` | `refactor: clean up utility folder structure and organize modules` \\u2014 Centralized `src/`, modular package domains, and test runner. |\\n| **Day 31** | Jan 31, 2026 | `docs` | `docs: add comprehensive README with architectural notes and interview cheat sheet` \\u2014 Master handbook and top 30 interview Q&As. |\\n\\n---\\n\\n## Directory Architecture\\n\\n```\\ntest1/\\n\\u251c\\u2500\\u2500 docs/                                # Technical Architectural Deep-Dives\\n\\u2502   \\u251c\\u2500\\u2500 day04-execution-context.md       # Execution contexts & call stack flow\\n\\u2502   \\u251c\\u2500\\u2500 day08-scope-chain.md             # Lexical scope & scope chains\\n\\u2502   \\u251c\\u2500\\u2500 day11-event-loop.md              # Event loop, microtask & macrotask queues\\n\\u2502   \\u251c\\u2500\\u2500 day15-async-generators.md        # Async/await vs Generator coroutines\\n\\u2502   \\u251c\\u2500\\u2500 day18-prototypal-inheritance.md  # Prototype delegation & __proto__\\n\\u2502   \\u251c\\u2500\\u2500 day21-this-keyword.md            # The 4 rules of \'this\' binding\\n\\u2502   \\u251c\\u2500\\u2500 day25-garbage-collection.md      # V8 Garbage collection & memory leaks\\n\\u2502   \\u2514\\u2500\\u2500 day28-weakmap-weakset-map-set.md # WeakMap, WeakSet, Map, and Set comparison\\n\\u251c\\u2500\\u2500 src/                                 # Production Modules\\n\\u2502   \\u251c\\u2500\\u2500 index.js                         # Master Barrel Export\\n\\u2502   \\u251c\\u2500\\u2500 polyfills/                       # Standard Built-in Polyfills\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 map.js                       # Array.prototype.map\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 filter.js                    # Array.prototype.filter\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 reduce.js                    # Array.prototype.reduce\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 call-apply-bind.js           # Function.prototype call/apply/bind\\n\\u2502   \\u2502   \\u2514\\u2500\\u2500 promise.js                   # Promises/A+ Custom Promise\\n\\u2502   \\u251c\\u2500\\u2500 async/                           # Asynchronous Patterns & Helpers\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 debounce.js                  # Debounce with leading/trailing\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 throttle.js                  # Throttle with timestamp/timer\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 execution-order.js           # Async execution tracing puzzles\\n\\u2502   \\u2502   \\u2514\\u2500\\u2500 promise-combinators.js       # Promise.all, race, allSettled\\n\\u2502   \\u251c\\u2500\\u2500 patterns/                        # Design & Architectural Patterns\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 closure-privacy.js           # Revealing Module Pattern\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 inheritance.js               # ES5 vs ES6 Inheritance\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 event-emitter.js             # Pub/Sub Event Emitter\\n\\u2502   \\u2502   \\u2514\\u2500\\u2500 reactive.js                  # Proxy & Reflect Reactive Store\\n\\u2502   \\u251c\\u2500\\u2500 utils/                           # General Utilities\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 hoisting.js                  # Hoisting demonstrations\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 memoize.js                   # Memoize with LRU Cache\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 deep-clone.js                # Deep clone with circular ref support\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 flatten.js                   # Object flatten & unflatten\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 curry.js                     # Currying with placeholders\\n\\u2502   \\u2502   \\u251c\\u2500\\u2500 compose-pipe.js              # Function composition & piping\\n\\u2502   \\u2502   \\u2514\\u2500\\u2500 lazy.js                      # Lazy property & thunk evaluation\\n\\u2502   \\u2514\\u2500\\u2500 puzzles/                         # Algorithmic Problem Solving\\n\\u2502       \\u2514\\u2500\\u2500 algorithms.js                # Sliding window, Two Sum, Kadane\'s\\n\\u251c\\u2500\\u2500 scripts/\\n\\u2502   \\u251c\\u2500\\u2500 test_runner.js                   # Automated test suite\\n\\u2502   \\u2514\\u2500\\u2500 generate_and_commit.js           # Git backdating generation script\\n\\u2514\\u2500\\u2500 commit_january_2026.ps1              # Windows PowerShell commit script\\n```\\n\\n---\\n\\n## Running Tests & Verifying Solutions\\n\\nTo run the entire suite of unit tests verifying all polyfills, utilities, and puzzles:\\n\\n```bash\\nnode scripts/test_runner.js\\n```\\n\\n---\\n\\n## High-Yield JavaScript Interview Cheat Sheet\\n\\n### 1. What is the difference between `==` and `===`?\\n- `==` (Abstract Equality) performs type coercion via the `ToPrimitive` algorithm before comparing.\\n- `===` (Strict Equality) checks both value and type without coercion.\\n\\n### 2. What is the Temporal Dead Zone (TDZ)?\\nThe period between entering the scope where a `let` or `const` variable is declared and the actual line where it is initialized. Accessing the variable in the TDZ throws a `ReferenceError`.\\n\\n### 3. How does JavaScript handle asynchronous code?\\nJavaScript uses an **Event Loop**. Synchronous code runs on the Call Stack. Asynchronous callbacks are registered with the runtime host and placed in either the **Microtask Queue** (`Promise`, `queueMicrotask`) or the **Macrotask Queue** (`setTimeout`, I/O). Microtasks are completely drained before the next macrotask executes.\\n\\n### 4. What is a Closure and what are its primary use cases?\\nA closure is a function bundled together with references to its surrounding lexical state. Even after the outer function finishes executing, the inner function retains access to outer variables. Key use cases: **Data Privacy / Encapsulation**, **Currying**, **Memoization**, and **Event Listeners**.\\n\\n### 5. What are the four rules of `this` binding?\\n1. **Default Binding**: Global object (`window` / `global`) or `undefined` in strict mode.\\n2. **Implicit Binding**: The object preceding the dot at call site (`user.getName()`).\\n3. **Explicit Binding**: Manually set using `.call()`, `.apply()`, or `.bind()`.\\n4. **`new` Binding**: Binds to the newly created instance inside a constructor function.\\n*(Note: Arrow functions do not follow these rules; they inherit `this` lexically from their enclosing scope).*\\n\\n### 6. What is the difference between `call`, `apply`, and `bind`?\\n- `call(thisArg, arg1, arg2, ...)`: Invokes immediately with comma-separated arguments.\\n- `apply(thisArg, [args])`: Invokes immediately with an array of arguments.\\n- `bind(thisArg, ...args)`: Returns a new function with `this` and initial arguments bound permanently.\\n\\n### 7. How does Prototypal Inheritance work?\\nEvery JavaScript object has an internal `[[Prototype]]` link. When a property or method is accessed on an object, JavaScript searches the object itself first. If missing, it traverses up the prototype chain until it either finds the property or reaches `null` at the top of `Object.prototype`.\\n\\n### 8. Explain the difference between Debounce and Throttle.\\n- **Debounce**: Delays execution until a specified delay has elapsed since the *last* time the function was invoked. (Ideal for search input autocomplete).\\n- **Throttle**: Ensures the function is called at most once within every specified time interval. (Ideal for scroll or resize handlers).\\n\\n### 9. What is a WeakMap and why would you use it over a Map?\\n`WeakMap` holds **weak references** to its keys, which must be objects. If an object key has no other references in memory, it can be garbage-collected even if it exists in the `WeakMap`. It is ideal for storing private metadata on DOM nodes or external objects without causing memory leaks.\\n"\n  }\n}')

ROOT_DIR = Path(__file__).resolve().parent

def run_cmd(cmd, env=None, check=True, capture_output=False):
    result = subprocess.run(
        cmd,
        cwd=ROOT_DIR,
        env=env or os.environ,
        shell=True,
        text=True,
        capture_output=capture_output
    )
    if check and result.returncode != 0:
        if capture_output:
            print(f"Command failed: {cmd}\n{result.stderr}", file=sys.stderr)
        sys.exit(result.returncode)
    return result

def ensure_parent(file_path):
    file_path.parent.mkdir(parents=True, exist_ok=True)

def write_file(rel_path, content):
    full_path = ROOT_DIR / rel_path
    ensure_parent(full_path)
    full_path.write_text(content.strip() + "\n", encoding="utf-8")

def remove_dir(rel_path):
    full_path = ROOT_DIR / rel_path
    if full_path.exists() and full_path.is_dir():
        shutil.rmtree(full_path, ignore_errors=True)

def git_commit(day_num, message, author_name="shoeb0023", author_email="sshoeb0023@gmail.com", dry_run=False):
    day_str = f"{day_num:02d}"
    date_str = f"2026-01-{day_str} 12:30:00 +0530"

    if dry_run:
        print(f"[DRY-RUN] Day {day_num}: '{message}' ({author_name} <{author_email}>) -> {date_str}")
        return

    # Selective staging
    if day_num < 30:
        if (ROOT_DIR / "days").exists():
            run_cmd("git add days/")
        if (ROOT_DIR / "docs").exists():
            run_cmd("git add docs/")
    else:
        run_cmd("git add -A")

    env = os.environ.copy()
    env["GIT_AUTHOR_NAME"] = author_name
    env["GIT_AUTHOR_EMAIL"] = author_email
    env["GIT_COMMITTER_NAME"] = author_name
    env["GIT_COMMITTER_EMAIL"] = author_email
    env["GIT_AUTHOR_DATE"] = date_str
    env["GIT_COMMITTER_DATE"] = date_str

    res = subprocess.run(
        ["git", "commit", "-m", message],
        cwd=ROOT_DIR,
        env=env,
        text=True,
        capture_output=True
    )
    if res.returncode == 0:
        print(f"[SUCCESS] Day {day_num} committed: {message} ({author_email}, {date_str})")
    else:
        if "nothing to commit" in res.stdout or "nothing to commit" in res.stderr:
            print(f"[NOTICE] Day {day_num} nothing new to commit.")
        else:
            print(f"[ERROR] Day {day_num} commit failed: {res.stderr.strip()}", file=sys.stderr)

def push_to_remote(remote="origin", branch="main", force=False):
    force_flag = " --force" if force else ""
    print(f"\nPushing commits to {remote}/{branch}{force_flag}...")
    res = run_cmd(f"git push {remote} {branch}{force_flag}", check=False, capture_output=False)
    if res.returncode == 0:
        print(f"\n[SUCCESS] Successfully pushed all 31 days to GitHub: {remote}/{branch}!")
    else:
        print(f"\n[ERROR] Failed to push to {remote}/{branch}. Please check network or GitHub credentials.", file=sys.stderr)

def main():
    parser = argparse.ArgumentParser(description="31-Day JavaScript Mastery Git Committer & Publisher")
    parser.add_argument("--author", default="shoeb0023", help="Git author name (default: shoeb0023)")
    parser.add_argument("--email", default="sshoeb0023@gmail.com", help="Git author email (default: sshoeb0023@gmail.com)")
    parser.add_argument("--push", action="store_true", help="Push commits to GitHub after completing")
    parser.add_argument("--force", action="store_true", help="Force push to remote branch")
    parser.add_argument("--push-only", action="store_true", help="Only push existing commits to GitHub without re-committing")
    parser.add_argument("--reset-to", default=None, help="Reset hard to specific base commit before running (e.g. 07dafc0)")
    parser.add_argument("--dry-run", action="store_true", help="Preview commit actions without running git")
    parser.add_argument("--branch", default="main", help="Target git branch (default: main)")
    parser.add_argument("--remote", default="origin", help="Target git remote (default: origin)")
    args = parser.parse_args()

    print("=" * 60)
    print("   31-DAY JAVASCRIPT MASTERY GIT COMMITTER (PYTHON)   ")
    print("   Target: January 1, 2026 - January 31, 2026         ")
    print(f"   Author: {args.author} <{args.email}>")
    print("=" * 60)

    if args.push_only:
        push_to_remote(args.remote, args.branch, args.force)
        return

    if args.reset_to:
        print(f"Resetting branch to {args.reset_to}...")
        run_cmd(f"git reset --hard {args.reset_to}")

    # Step 1: Days 1 to 29
    for day_num, msg, files in PAYLOAD["days_1_to_29"]:
        print(f"\n--- Processing Day {day_num} ---")
        if not args.dry_run:
            for rel_path, content in files.items():
                write_file(rel_path, content)
        git_commit(day_num, msg, args.author, args.email, args.dry_run)

    # Step 2: Day 30 - Refactor into src/ and docs/
    print(f"\n--- Processing Day 30 ---")
    msg_30 = "refactor: clean up utility folder structure and organize modules"
    if not args.dry_run:
        for rel_path, content in PAYLOAD["day30_files"].items():
            write_file(rel_path, content)
        remove_dir("days")
    git_commit(30, msg_30, args.author, args.email, args.dry_run)

    # Step 3: Day 31 - Master README & Interview Cheat Sheet
    print(f"\n--- Processing Day 31 ---")
    msg_31 = "docs: add comprehensive README with architectural notes and interview cheat sheet"
    if not args.dry_run:
        for rel_path, content in PAYLOAD["day31_files"].items():
            write_file(rel_path, content)
    git_commit(31, msg_31, args.author, args.email, args.dry_run)

    print("\n" + "=" * 60)
    print("   ALL 31 DAYS GENERATED & COMMITTED!   ")
    print("=" * 60)

    # Run test runner if node is present
    if not args.dry_run:
        print("\nRunning test runner verification...")
        run_cmd("node scripts/test_runner.js", check=False)

    # Push if requested
    if args.push:
        push_to_remote(args.remote, args.branch, args.force)
    else:
        print(f"\nTo push your commits to GitHub, run:")
        print(f"    git push {args.remote} {args.branch}" + (" --force" if args.force else ""))
        print(f"Or re-run this script with --push:")
        print(f"    python commit_31_days.py --push --force")

if __name__ == "__main__":
    main()
